mutation insertUser($user: users_insert_input!) {
  insertUser(object: $user) {
    id
    email
    displayName
    avatarURL
    locale
  }
}

mutation deleteUser($id: uuid!) {
  deleteUser(id: $id) {
    id
  }
}
