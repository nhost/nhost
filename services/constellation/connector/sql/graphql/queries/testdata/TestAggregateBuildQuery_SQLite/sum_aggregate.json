[
  {
    "Name": "departments_aggregate",
    "QueryType": "select_aggregate",
    "SQL": "WITH \"_root.base\" AS (SELECT * FROM \"departments\") SELECT json_object('aggregate', (SELECT json_object('sum', json_object('budget', SUM(\"budget\"))) FROM \"_root.base\")) AS \"departments_aggregate\"",
    "Parameters": []
  }
]