[
  {
    "Name": "departments_aggregate",
    "SQL": "WITH \"_root.base\" AS (SELECT * FROM \"departments\") SELECT json_object('aggregate', (SELECT json_object('a', json_object('budget', SUM(\"budget\")), 'b', json_object('budget', SUM(\"budget\"))) FROM \"_root.base\")) AS \"departments_aggregate\"",
    "Parameters": [],
    "StreamCursors": null
  }
]