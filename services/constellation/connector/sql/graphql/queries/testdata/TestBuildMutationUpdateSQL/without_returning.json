[
  {
    "Name": "updateUsers",
    "QueryType": "update",
    "SQL": "WITH mutation_result AS (UPDATE \"auth\".\"users\" SET \"locale\" = $1::varchar WHERE users.\"disabled\" = $2::bool RETURNING *) SELECT json_build_object('affected_rows', (SELECT COUNT(*) FROM mutation_result))",
    "Parameters": [
      "en",
      false
    ]
  }
]