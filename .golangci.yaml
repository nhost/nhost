---
linters:
    enable-all: true
    disable:
      # - gochecknoglobals
      # - gochecknoinits
      # - lll
      # - gomnd
      # - gofumpt
      # - paralleltest
      # - testpackage
      # - forbidigo
      - gomoddirectives
      - nlreturn
      - wsl
      - exhaustivestruct
      - maligned  # deprecated
      - scopelint  # deprecated
      - interfacer  # deprecated
      - golint  # deprecated

issues:
  exclude-rules:
    - linters:
        - lll
      source: "^//go:generate "
    - path: controller/version.go
      linters:
        - gochecknoglobals
    - path: _test\.go
      linters:
        - funlen
        - ireturn
        - varnamelen
    - text: "NopCloser not declared by package io" # needs to go away
      linters:
        - typecheck
    - text: "ReadAll not declared by package io"   # needs to go away
      linters:
        - typecheck
    - text: "New not declared by package iofs"     # needs to go away
      linters:
        - typecheck

