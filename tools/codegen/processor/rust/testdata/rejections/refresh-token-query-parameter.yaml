openapi: 3.0.0
info:
  title: Invalid refresh token with a query parameter
  version: 1.0.0
paths:
  /token:
    post:
      operationId: refreshToken
      parameters:
        - name: trace
          in: query
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken:
                  type: string
              required:
                - refreshToken
      responses:
        "204":
          description: Refreshed
