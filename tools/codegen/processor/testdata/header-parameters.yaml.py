# This file is auto-generated by tools/codegen. Do not edit manually.
# ruff: noqa
# mypy: ignore-errors
from __future__ import annotations

import json
from typing import Any, Literal

import httpx
from pydantic import BaseModel, ConfigDict, Field

from ..fetch import (
    ChainFunction,
    FetchError,
    FetchResponse,
    UploadFile,
    create_enhanced_fetch,
    decode_json,
    to_file_part,
    to_json,
    to_jsonable,
)

def _parameter_scalar(value: Any) -> str:
    if value is None:
        return ""
    if isinstance(value, str):
        return value
    if isinstance(value, bool):
        return "true" if value else "false"
    if isinstance(value, (dict, list)):
        return json.dumps(value, separators=(",", ":"))
    return str(value)

def _header_value(value: Any, explode: bool) -> str:
    value = to_jsonable(value)
    if isinstance(value, list):
        return ",".join(_parameter_scalar(item) for item in value)
    if isinstance(value, dict):
        parts: list[str] = []
        for key, item in value.items():
            if explode:
                parts.append(f"{key}={_parameter_scalar(item)}")
            else:
                parts.extend((key, _parameter_scalar(item)))
        return ",".join(parts)
    return _parameter_scalar(value)


GetXMode = Literal[0, "one", True]


class HeaderValuesParams(BaseModel):
    model_config = ConfigDict(populate_by_name=True)


    x_first_json: str = Field(alias="x-first-json")
    x_second_json: bool = Field(alias="x-second-json")
    x_count: int = Field(alias="x-count")
    x_number: float = Field(alias="x-number")
    x_enabled: bool = Field(alias="x-enabled")
    x_mode: GetXMode = Field(alias="x-mode")


class Client:
    """Generated async API client backed by an httpx.AsyncClient and a middleware chain."""

    def __init__(
        self,
        base_url: str,
        chain_functions: list[ChainFunction] | None = None,
        http_client: httpx.AsyncClient | None = None,
    ) -> None:
        self.base_url = base_url
        self._chain_functions: list[ChainFunction] = list(chain_functions or [])
        self._http = http_client if http_client is not None else httpx.AsyncClient()
        self._fetch = create_enhanced_fetch(self._http, self._chain_functions)

    def push_chain_function(self, chain_function: ChainFunction) -> None:
        """Append a middleware chain function and rebuild the fetch pipeline."""
        self._chain_functions.append(chain_function)
        self._fetch = create_enhanced_fetch(self._http, self._chain_functions)


    async def header_values(
        self,
        params: HeaderValuesParams,
        headers: dict[str, str] | None = None,
    ) -> FetchResponse[None]:
        url = f"{self.base_url}/headers"
        query = None

        _parameter_headers: dict[str, str] = {}
        _parameter_headers["x-first-json"] = to_json(params.x_first_json)
        _parameter_headers["x-second-json"] = to_json(params.x_second_json)
        _parameter_headers["x-count"] = _header_value(params.x_count, False)
        _parameter_headers["x-number"] = _header_value(params.x_number, False)
        _parameter_headers["x-enabled"] = _header_value(params.x_enabled, False)
        _parameter_headers["x-mode"] = _header_value(params.x_mode, False)
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers={**_parameter_headers, **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= 300:
            raise FetchError.from_response(response)
        payload = None
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


    def header_only_redirect_url(
        self,
    ) -> str:
        url = f"{self.base_url}/redirect"
        return url


def create_api_client(
    base_url: str,
    chain_functions: list[ChainFunction] | None = None,
    http_client: httpx.AsyncClient | None = None,
) -> Client:
    """Create a new API client."""
    return Client(base_url, chain_functions, http_client)

__all__ = [
    "GetXMode",
    "HeaderValuesParams",
    "Client",
    "create_api_client",
]
