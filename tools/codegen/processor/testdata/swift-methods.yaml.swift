// -----------------------------------------------------------------------------
// <auto-generated>
//     This file is auto-generated. Do not edit manually.
// </auto-generated>
// -----------------------------------------------------------------------------

import Foundation
// Generated model namespace prefix: Fixture

public struct FixtureCreateSessionRequest: Codable, Sendable {
    public let email: String
    public let verificationCode: String
    public let requiredNullable: String?
    public let rememberMe: Bool?
    public let metadata: [String: JSONValue]?
    public let requestedAt: Date?

    public init(
        email: String,
        verificationCode: String,
        requiredNullable: String?,
        rememberMe: Bool? = nil,
        metadata: [String: JSONValue]? = nil,
        requestedAt: Date? = nil
    ) {
        self.email = email
        self.verificationCode = verificationCode
        self.requiredNullable = requiredNullable
        self.rememberMe = rememberMe
        self.metadata = metadata
        self.requestedAt = requestedAt
    }

    private enum CodingKeys: String, CodingKey {
        case email
        case verificationCode = "verification-code"
        case requiredNullable = "required_nullable"
        case rememberMe = "remember-me"
        case metadata
        case requestedAt = "requested-at"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(email, forKey: .email)
        try container.encode(verificationCode, forKey: .verificationCode)
        try container.encode(requiredNullable, forKey: .requiredNullable)
        try container.encodeIfPresent(rememberMe, forKey: .rememberMe)
        try container.encodeIfPresent(metadata, forKey: .metadata)
        try container.encodeIfPresent(requestedAt, forKey: .requestedAt)
    }
}

public struct FixtureSession: Codable, Sendable {
    public let id: String
    public let createdAt: Date
    public let roles: [String]

    public init(
        id: String,
        createdAt: Date,
        roles: [String]
    ) {
        self.id = id
        self.createdAt = createdAt
        self.roles = roles
    }

    private enum CodingKeys: String, CodingKey {
        case id
        case createdAt
        case roles
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.decode(String.self, forKey: .id)
        createdAt = try container.decode(Date.self, forKey: .createdAt)
        roles = try container.decodeIfPresent([String].self, forKey: .roles) ?? []
    }
}

public struct FixtureFileMetadata: Codable, Sendable {
    public let fileId: String?
    public let metadata: [String: JSONValue]?

    public init(
        fileId: String? = nil,
        metadata: [String: JSONValue]? = nil
    ) {
        self.fileId = fileId
        self.metadata = metadata
    }

    private enum CodingKeys: String, CodingKey {
        case fileId = "file-id"
        case metadata
    }
}

public struct FixtureUploadResult: Codable, Sendable {
    public let processedFiles: [FixtureFileMetadata]?

    public init(
        processedFiles: [FixtureFileMetadata]? = nil
    ) {
        self.processedFiles = processedFiles
    }
}

public struct FixtureErrorResponse: Codable, Sendable {
    public let message: String
    public let code: String?

    public init(
        message: String,
        code: String? = nil
    ) {
        self.message = message
        self.code = code
    }
}

public struct FixturePostCompact: Codable, Sendable {
    public let ownerId: String?
    public let active: Bool?

    public init(
        ownerId: String? = nil,
        active: Bool? = nil
    ) {
        self.ownerId = ownerId
        self.active = active
    }

    private enum CodingKeys: String, CodingKey {
        case ownerId = "owner-id"
        case active
    }
}

public struct FixturePostFilter: Codable, Sendable {
    public let ownerId: String?
    public let limit: Int?

    public init(
        ownerId: String? = nil,
        limit: Int? = nil
    ) {
        self.ownerId = ownerId
        self.limit = limit
    }

    private enum CodingKeys: String, CodingKey {
        case ownerId = "owner-id"
        case limit
    }
}

public struct FixtureExchangeTokenBodyOptions: Codable, Sendable {
    public let redirectTo: String?
    public let scope: String?

    public init(
        redirectTo: String? = nil,
        scope: String? = nil
    ) {
        self.redirectTo = redirectTo
        self.scope = scope
    }
}

public struct FixtureExchangeTokenBody: Codable, Sendable {
    public let clientId: String
    public let refreshToken: String
    public let options: FixtureExchangeTokenBodyOptions?

    public init(
        clientId: String,
        refreshToken: String,
        options: FixtureExchangeTokenBodyOptions? = nil
    ) {
        self.clientId = clientId
        self.refreshToken = refreshToken
        self.options = options
    }

    private enum CodingKeys: String, CodingKey {
        case clientId = "client-id"
        case refreshToken
        case options
    }
}

public struct FixtureUploadFilesBody: Codable, Sendable {
    public let bucketId: String?
    public let file: [Data]
    public let metadata: [FixtureFileMetadata]?

    public init(
        bucketId: String? = nil,
        file: [Data],
        metadata: [FixtureFileMetadata]? = nil
    ) {
        self.bucketId = bucketId
        self.file = file
        self.metadata = metadata
    }

    private enum CodingKeys: String, CodingKey {
        case bucketId = "bucket-id"
        case file = "file[]"
        case metadata = "metadata[]"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        bucketId = try container.decodeIfPresent(String.self, forKey: .bucketId)
        file = try container.decodeIfPresent([Data].self, forKey: .file) ?? []
        metadata = try container.decodeIfPresent([FixtureFileMetadata].self, forKey: .metadata)
    }
}

public struct FixtureGetFirstFilter: Codable, Sendable {
    public let ownerId: String?
    public let limit: Int?

    public init(
        ownerId: String? = nil,
        limit: Int? = nil
    ) {
        self.ownerId = ownerId
        self.limit = limit
    }

    private enum CodingKeys: String, CodingKey {
        case ownerId = "owner-id"
        case limit
    }
}

public struct FixtureGetSecondFilter: Codable, Sendable {
    public let active: Bool?
    public let tag: String?

    public init(
        active: Bool? = nil,
        tag: String? = nil
    ) {
        self.active = active
        self.tag = tag
    }
}

public struct FixtureCreateSessionQuery: Sendable {
    public let compact: FixturePostCompact?
    public let filter: FixturePostFilter?
    public let ids: [String]?
    public let include: [String]?
    public let metadata: [String: JSONValue]?
    public let redirectTo: String?

    public init(
        compact: FixturePostCompact? = nil,
        filter: FixturePostFilter? = nil,
        ids: [String]? = nil,
        include: [String]? = nil,
        metadata: [String: JSONValue]? = nil,
        redirectTo: String? = nil
    ) {
        self.compact = compact
        self.filter = filter
        self.ids = ids
        self.include = include
        self.metadata = metadata
        self.redirectTo = redirectTo
    }
}

public struct FixtureCreateSessionHeaders: Sendable {
    public let ifMatch: [String]?
    public let xHasuraRole: String?

    public init(
        ifMatch: [String]? = nil,
        xHasuraRole: String? = nil
    ) {
        self.ifMatch = ifMatch
        self.xHasuraRole = xHasuraRole
    }
}

public struct FixtureDownloadFileQuery: Sendable {
    public let transform: String?

    public init(
        transform: String? = nil
    ) {
        self.transform = transform
    }
}

public struct FixtureSearchThingsQuery: Sendable {
    public let firstFilter: FixtureGetFirstFilter
    public let secondFilter: FixtureGetSecondFilter

    public init(
        firstFilter: FixtureGetFirstFilter,
        secondFilter: FixtureGetSecondFilter
    ) {
        self.firstFilter = firstFilter
        self.secondFilter = secondFilter
    }
}

public struct FixtureVerifyTicketQuery: Sendable {
    public let redirectTo: String
    public let ticket: String

    public init(
        redirectTo: String,
        ticket: String
    ) {
        self.redirectTo = redirectTo
        self.ticket = ticket
    }
}

public struct GeneratedFixtureClient: Sendable {
    public let baseURL: URL
    // Internal (not private) so hand-written same-module extensions, e.g. the
    // streaming Storage upload, can reuse the configured middleware pipeline.
    let fetch: FetchFunction

    public init(baseURL: URL, fetch: @escaping FetchFunction) {
        self.baseURL = baseURL
        self.fetch = fetch
    }

    public init(
        baseURL: URL,
        transport: any HTTPTransport = URLSessionTransport(),
        middleware: [ChainFunction] = []
    ) {
        self.init(
            baseURL: baseURL,
            fetch: NhostFetchPipeline(transport: transport, middleware: middleware).fetch
        )
    }

    public func createSession(
        ticketId: String,
        body: FixtureCreateSessionRequest,
        query: FixtureCreateSessionQuery? = nil,
        headers: FixtureCreateSessionHeaders? = nil,
        extraHeaders: [String: String] = [:]
    ) async throws -> NhostResponse<FixtureSession> {
        let ticketIdPath = try NhostURLBuilder.percentEncodePathSegment(NhostWireEncoder.string(ticketId))
        let path = "/session/\(ticketIdPath)"
        var queryItems: [String: JSONValue?] = [:]
        if let query {
            queryItems["compact"] = try query.compact.map { JSONValue.string(try NhostWireEncoder.string($0)) }
            if let value = query.filter {
                queryItems["limit"] = try value.limit.map { try NhostWireEncoder.jsonValue($0) }
                queryItems["owner-id"] = try value.ownerId.map { try NhostWireEncoder.jsonValue($0) }
            }
            queryItems["ids"] = try query.ids.map { JSONValue.string(try NhostWireEncoder.commaSeparated($0)) }
            queryItems["include[]"] = try query.include.map { try NhostWireEncoder.jsonValue($0) }
            queryItems["metadata"] = try query.metadata.map { JSONValue.string(try NhostWireEncoder.jsonString($0)) }
            queryItems["redirect-to"] = try query.redirectTo.map { try NhostWireEncoder.jsonValue($0) }
        }
        let url = NhostURLBuilder.url(baseURL: baseURL, path: path, query: queryItems)
        var requestHeaders = [
            "accept": "application/json",
        ]
        requestHeaders["content-type"] = "application/json"
        let requestBody = try NhostJSON.restEncoder.encode(body)
        var headerValues: [String: JSONValue?] = [:]
        if let headers {
            headerValues["if-match"] = try headers.ifMatch.map { try NhostWireEncoder.jsonValue($0) }
            headerValues["x-hasura-role"] = try headers.xHasuraRole.map { try NhostWireEncoder.jsonValue($0) }
        }
        requestHeaders = NhostHeaderEncoder.merge(base: requestHeaders, values: headerValues)
        requestHeaders = NhostHeaderEncoder.merge(base: requestHeaders, overrides: extraHeaders)
        let request = NhostRequest(
            method: "POST",
            url: url,
            headers: requestHeaders,
            body: requestBody
        )
        let response = try await fetch(request)

        return try NhostHTTP.decodeResponse(FixtureSession.self, from: response)
    }

    public func exchangeToken(
        body: FixtureExchangeTokenBody,
        extraHeaders: [String: String] = [:]
    ) async throws -> NhostResponse<Void> {
        let path = "/token"
        let url = NhostURLBuilder.url(baseURL: baseURL, path: path)
        var requestHeaders = [
            "accept": "application/json",
        ]
        requestHeaders["content-type"] = NhostURLEncodedFormEncoder.contentType
        let formFields: [String: JSONValue?] = [
            "client-id": try NhostWireEncoder.jsonValue(body.clientId),
            "options": try body.options.map { try NhostWireEncoder.jsonValue($0) },
            "refreshToken": try NhostWireEncoder.jsonValue(body.refreshToken)
        ]
        let requestBody = NhostURLEncodedFormEncoder.encode(formFields)
        requestHeaders = NhostHeaderEncoder.merge(base: requestHeaders, overrides: extraHeaders)
        let request = NhostRequest(
            method: "POST",
            url: url,
            headers: requestHeaders,
            body: requestBody
        )
        let response = try await fetch(request)

        return try NhostHTTP.emptyResponse(from: response)
    }

    public func downloadFile(
        id: String,
        query: FixtureDownloadFileQuery? = nil,
        extraHeaders: [String: String] = [:]
    ) async throws -> NhostResponse<Data> {
        let idPath = try NhostURLBuilder.percentEncodePathSegment(NhostWireEncoder.string(id))
        let path = "/files/\(idPath)"
        var queryItems: [String: JSONValue?] = [:]
        if let query {
            queryItems["transform"] = try query.transform.map { try NhostWireEncoder.jsonValue($0) }
        }
        let url = NhostURLBuilder.url(baseURL: baseURL, path: path, query: queryItems)
        var requestHeaders = [
            "accept": "application/octet-stream",
        ]
        let requestBody: Data? = nil
        requestHeaders = NhostHeaderEncoder.merge(base: requestHeaders, overrides: extraHeaders)
        let request = NhostRequest(
            method: "GET",
            url: url,
            headers: requestHeaders,
            body: requestBody
        )
        let response = try await fetch(request)

        return try NhostHTTP.binaryResponse(from: response)
    }

    public func uploadFiles(
        body: FixtureUploadFilesBody,
        extraHeaders: [String: String] = [:]
    ) async throws -> NhostResponse<FixtureUploadResult> {
        let path = "/files"
        let url = NhostURLBuilder.url(baseURL: baseURL, path: path)
        var requestHeaders = [
            "accept": "application/json",
        ]
        var parts: [NhostMultipartPart] = []
        if let value = body.bucketId {
            parts.append(.formField(name: "bucket-id", value: try NhostWireEncoder.jsonValue(value)))
        }
        for item in body.file {
            parts.append(NhostMultipartPart.file(name: "file[]", filename: "blob", contentType: NhostBinaryBody.contentType, data: item))
        }
        if let value = body.metadata {
            for item in value {
                parts.append(NhostMultipartPart(name: "metadata[]", contentType: "application/json", body: try NhostJSON.restEncoder.encode(item)))
            }
        }
        let multipartBody = try NhostMultipartEncoder.encode(parts: parts)
        requestHeaders["content-type"] = multipartBody.contentType
        let requestBody = multipartBody.body
        requestHeaders = NhostHeaderEncoder.merge(base: requestHeaders, overrides: extraHeaders)
        let request = NhostRequest(
            method: "POST",
            url: url,
            headers: requestHeaders,
            body: requestBody
        )
        let response = try await fetch(request)

        return try NhostHTTP.decodeResponse(FixtureUploadResult.self, from: response)
    }

    public func searchThings(
        query: FixtureSearchThingsQuery,
        extraHeaders: [String: String] = [:]
    ) async throws -> NhostResponse<FixtureSession> {
        let path = "/search"
        var queryItems: [String: JSONValue?] = [:]
        do {
            let value = query.firstFilter
            queryItems["limit"] = try value.limit.map { try NhostWireEncoder.jsonValue($0) }
            queryItems["owner-id"] = try value.ownerId.map { try NhostWireEncoder.jsonValue($0) }
        }
        do {
            let value = query.secondFilter
            queryItems["active"] = try value.active.map { try NhostWireEncoder.jsonValue($0) }
            queryItems["tag"] = try value.tag.map { try NhostWireEncoder.jsonValue($0) }
        }
        let url = NhostURLBuilder.url(baseURL: baseURL, path: path, query: queryItems)
        var requestHeaders = [
            "accept": "application/json",
        ]
        let requestBody: Data? = nil
        requestHeaders = NhostHeaderEncoder.merge(base: requestHeaders, overrides: extraHeaders)
        let request = NhostRequest(
            method: "GET",
            url: url,
            headers: requestHeaders,
            body: requestBody
        )
        let response = try await fetch(request)

        return try NhostHTTP.decodeResponse(FixtureSession.self, from: response)
    }

    public func verifyTicketURL(
        query: FixtureVerifyTicketQuery
    ) throws -> URL {
        let path = "/verify"
        var queryItems: [String: JSONValue?] = [:]
        queryItems["redirectTo"] = try NhostWireEncoder.jsonValue(query.redirectTo)
        queryItems["ticket"] = try NhostWireEncoder.jsonValue(query.ticket)
        return NhostURLBuilder.redirectURL(baseURL: baseURL, path: path, query: queryItems)
    }
}
