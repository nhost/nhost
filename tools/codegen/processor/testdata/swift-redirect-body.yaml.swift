// -----------------------------------------------------------------------------
// <auto-generated>
//     This file is auto-generated. Do not edit manually.
// </auto-generated>
// -----------------------------------------------------------------------------

import Foundation
// Generated model namespace prefix: Redirect

public struct RedirectAuthorizePostBody: Codable, Sendable {
    public let clientId: String
    public let redirectUri: String?

    public init(
        clientId: String,
        redirectUri: String? = nil
    ) {
        self.clientId = clientId
        self.redirectUri = redirectUri
    }

    private enum CodingKeys: String, CodingKey {
        case clientId = "client_id"
        case redirectUri = "redirect_uri"
    }
}

public struct RedirectClient: Sendable {
    public let baseURL: URL
    // Internal (not private) so hand-written same-module extensions, e.g. the
    // streaming Storage upload, can reuse the configured middleware pipeline.
    let fetch: FetchFunction

    public init(baseURL: URL, fetch: @escaping FetchFunction) {
        self.baseURL = baseURL
        self.fetch = NhostFetchPipeline(fetch: fetch).fetch
    }

    public init(
        baseURL: URL,
        transport: any HTTPTransport = URLSessionTransport(),
        middleware: [ChainFunction] = []
    ) {
        self.baseURL = baseURL
        self.fetch = NhostFetchPipeline(transport: transport, middleware: middleware).fetch
    }

    public func authorizePostURL(
        body: RedirectAuthorizePostBody
    ) throws -> URL {
        let path = "/authorize"
        var queryItems: [String: JSONValue?] = [:]
        queryItems["client_id"] = try NhostWireEncoder.jsonValue(body.clientId)
        queryItems["redirect_uri"] = try body.redirectUri.map { try NhostWireEncoder.jsonValue($0) }
        return NhostURLBuilder.redirectURL(baseURL: baseURL, path: path, query: queryItems)
    }
}
