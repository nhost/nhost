// -----------------------------------------------------------------------------
// <auto-generated>
//     This file is auto-generated. Do not edit manually.
// </auto-generated>
// -----------------------------------------------------------------------------

import Foundation
// Generated model namespace prefix: Test

/// Stable external identifier.
public typealias TestExternalID = String

/// RFC3339 timestamp.
public typealias TestTimestamp = Date

/// Binary payload bytes.
public typealias TestBinaryPayload = Data

/// Current account status.
public enum TestAccountStatus: String, Codable, Sendable {
    case active = "active"
    case inProgress = "in-progress"
    case `default` = "default"
    case value2faEnabled = "2fa-enabled"
}

/// Integer-backed tier.
public enum TestAccountTier: Int, Codable, Sendable {
    case value0 = 0
    case value1 = 1
    case value404 = 404
}

/// Nested owner object.
public struct TestAccountOwner: Codable, Sendable {
    /// Owner user identifier.
    public let userId: String
    /// Owner email.
    public let email: String?

    public init(
        userId: String,
        email: String? = nil
    ) {
        self.userId = userId
        self.email = email
    }

    private enum CodingKeys: String, CodingKey {
        case userId = "user-id"
        case email
    }
}

/// Account model with Swift identifier edge cases.
public struct TestAccount: Codable, Sendable {
    /// Unique identifier.
    public let id: String
    /// Optional display name.
    public let displayName: String?
    /// Required nullable value.
    public let requiredNullable: String?
    /// Reserved Swift keyword property.
    public let `class`: String
    /// Creation time.
    public let createdAt: Date
    /// Avatar bytes.
    public let avatar: Data
    /// Login count.
    public let loginCount: Int
    /// Account score.
    public let score: Double
    /// Whether the account is active.
    public let active: Bool
    /// Account tags.
    public let tags: [String]
    /// Untyped JSON preferences.
    public let preferences: [String: JSONValue]
    /// Labels with explicit Swift map override.
    public let localizedLabels: [String: String]
    /// Uploaded file payloads.
    public let file: [Data]?
    /// Current account status.
    public let status: TestAccountStatus
    /// Integer-backed tier.
    public let tier: TestAccountTier
    /// Nested owner object.
    public let owner: TestAccountOwner?

    public init(
        id: String,
        displayName: String? = nil,
        requiredNullable: String?,
        `class`: String,
        createdAt: Date,
        avatar: Data,
        loginCount: Int,
        score: Double,
        active: Bool,
        tags: [String],
        preferences: [String: JSONValue],
        localizedLabels: [String: String],
        file: [Data]? = nil,
        status: TestAccountStatus,
        tier: TestAccountTier,
        owner: TestAccountOwner? = nil
    ) {
        self.id = id
        self.displayName = displayName
        self.requiredNullable = requiredNullable
        self.`class` = `class`
        self.createdAt = createdAt
        self.avatar = avatar
        self.loginCount = loginCount
        self.score = score
        self.active = active
        self.tags = tags
        self.preferences = preferences
        self.localizedLabels = localizedLabels
        self.file = file
        self.status = status
        self.tier = tier
        self.owner = owner
    }

    private enum CodingKeys: String, CodingKey {
        case id
        case displayName = "display_name"
        case requiredNullable = "required_nullable"
        case `class`
        case createdAt = "created-at"
        case avatar
        case loginCount = "login-count"
        case score
        case active
        case tags
        case preferences
        case localizedLabels
        case file = "file[]"
        case status
        case tier
        case owner
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.decode(String.self, forKey: .id)
        displayName = try container.decodeIfPresent(String.self, forKey: .displayName)
        requiredNullable = try container.decodeIfPresent(String.self, forKey: .requiredNullable)
        `class` = try container.decode(String.self, forKey: .`class`)
        createdAt = try container.decode(Date.self, forKey: .createdAt)
        avatar = try container.decode(Data.self, forKey: .avatar)
        loginCount = try container.decode(Int.self, forKey: .loginCount)
        score = try container.decode(Double.self, forKey: .score)
        active = try container.decode(Bool.self, forKey: .active)
        tags = try container.decodeIfPresent([String].self, forKey: .tags) ?? []
        preferences = try container.decodeIfPresent([String: JSONValue].self, forKey: .preferences) ?? [:]
        localizedLabels = try container.decodeIfPresent([String: String].self, forKey: .localizedLabels) ?? [:]
        file = try container.decodeIfPresent([Data].self, forKey: .file)
        status = try container.decode(TestAccountStatus.self, forKey: .status)
        tier = try container.decode(TestAccountTier.self, forKey: .tier)
        owner = try container.decodeIfPresent(TestAccountOwner.self, forKey: .owner)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encodeIfPresent(displayName, forKey: .displayName)
        try container.encode(requiredNullable, forKey: .requiredNullable)
        try container.encode(`class`, forKey: .`class`)
        try container.encode(createdAt, forKey: .createdAt)
        try container.encode(avatar, forKey: .avatar)
        try container.encode(loginCount, forKey: .loginCount)
        try container.encode(score, forKey: .score)
        try container.encode(active, forKey: .active)
        try container.encode(tags, forKey: .tags)
        try container.encode(preferences, forKey: .preferences)
        try container.encode(localizedLabels, forKey: .localizedLabels)
        try container.encodeIfPresent(file, forKey: .file)
        try container.encode(status, forKey: .status)
        try container.encode(tier, forKey: .tier)
        try container.encodeIfPresent(owner, forKey: .owner)
    }
}
