# This file is auto-generated by tools/codegen. Do not edit manually.
from __future__ import annotations

from collections.abc import Mapping, Sequence
from datetime import date
from typing import Any
from uuid import UUID

import httpx
from pydantic import BaseModel, ConfigDict

from ..fetch import (
    FetchResponse,
    HTTPError,
    Middleware,
    UploadFile,
    create_fetch_pipeline,
    decode_json,
)

_MIN_ERROR_STATUS = 300


class Marker(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    value: str


class Client:
    """Generated async API client backed by an httpx.AsyncClient and a middleware chain."""

    def __init__(
        self,
        base_url: str,
        *,
        middleware: Sequence[Middleware] = (),
        http_client: httpx.AsyncClient | None = None,
    ) -> None:
        self.base_url = base_url
        self._middleware = list(middleware)
        self._owns_http_client = http_client is None
        self._http = http_client if http_client is not None else httpx.AsyncClient()
        self._fetch = create_fetch_pipeline(self._http, self._middleware)

    async def __aenter__(self) -> Client:
        return self

    async def __aexit__(self, *_: object) -> None:
        await self.aclose()

    async def aclose(self) -> None:
        """Close the internally owned HTTP client, if any."""
        if self._owns_http_client:
            await self._http.aclose()

    def add_middleware(self, middleware: Middleware) -> None:
        """Append HTTP middleware and rebuild the request pipeline."""
        self._middleware.append(middleware)
        self._fetch = create_fetch_pipeline(self._http, self._middleware)


    async def get_untyped_inline(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[dict[str, Any]]:
        (
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[dict[str, Any]]: The HTTP response."""
        )
        url = f"{self.base_url}/untyped-inline"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, dict[str, Any])
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


    async def get_untyped_reference(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[dict[str, Any]]:
        (
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[dict[str, Any]]: The HTTP response."""
        )
        url = f"{self.base_url}/untyped-reference"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, dict[str, Any])
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


    async def get_untyped_array(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[list[Any]]:
        (
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[list[Any]]: The HTTP response."""
        )
        url = f"{self.base_url}/untyped-array"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, list[Any])
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


    async def get_binary_array(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[list[bytes | UploadFile]]:
        (
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[list[bytes | UploadFile]]: The HTTP response."""
        )
        url = f"{self.base_url}/binary-array"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, list[bytes | UploadFile])
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


    async def get_uuid(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[UUID]:
        (
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[UUID]: The HTTP response."""
        )
        url = f"{self.base_url}/uuid"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, UUID)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


    async def get_date(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[date]:
        (
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[date]: The HTTP response."""
        )
        url = f"{self.base_url}/date"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, date)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


__all__ = [
    "Marker",
    "Client",
]
