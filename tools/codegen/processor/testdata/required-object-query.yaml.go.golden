// Code generated by tools/codegen. DO NOT EDIT.

package testpkg

import (
	"context"
	"encoding/json"
	"fmt"
	"net/http"
	"net/url"

	"github.com/nhost/nhost/packages/nhost-go/transport"
)

type Filter struct {
	Term string `json:"term"`
}

type GetItemsParams struct {
	Filter Filter `json:"filter"`
}

func (p *GetItemsParams) toQuery() url.Values {
	q := url.Values{}
	{
		encoded, _ := json.Marshal(p.Filter)
		var fields map[string]any
		if json.Unmarshal(encoded, &fields) == nil {
			for k, v := range fields {
				q.Add(k, fmt.Sprint(v))
			}
		}
	}
	return q
}

type GetOptionalItemsParams struct {
	Filter *Filter `json:"filter,omitempty"`
}

func (p *GetOptionalItemsParams) toQuery() url.Values {
	q := url.Values{}
	if p.Filter != nil {
		{
			encoded, _ := json.Marshal(*p.Filter)
			var fields map[string]any
			if json.Unmarshal(encoded, &fields) == nil {
				for k, v := range fields {
					q.Add(k, fmt.Sprint(v))
				}
			}
		}
	}
	return q
}

// Client is a generated API client backed by an *http.Client. Install request
// middleware (session refresh, token attachment, ...) via the client's
// Transport; see transport.NewHTTPClient.
type Client struct {
	BaseURL    string
	httpClient *http.Client
}

// NewClient creates a new API client. A nil httpClient uses a default
// *http.Client; supply one whose Transport carries the desired middleware.
func NewClient(baseURL string, httpClient *http.Client) *Client {
	if httpClient == nil {
		httpClient = &http.Client{}
	}

	return &Client{
		BaseURL:    baseURL,
		httpClient: httpClient,
	}
}

// GetItems performs GET /items. It returns the decoded body,
// the HTTP response metadata, and a *transport.APIError for non-2xx/3xx
// responses.
func (c *Client) GetItems(
	ctx context.Context,
	params GetItemsParams,
	headers http.Header,
) (json.RawMessage, *transport.Response, error) {
	var payload json.RawMessage
	u := fmt.Sprintf("%s/items", c.BaseURL)
	req, err := http.NewRequestWithContext(ctx, "GET", u, nil)
	if err != nil {
		return payload, nil, err
	}
	req.URL.RawQuery = params.toQuery().Encode()
	for k, vs := range headers {
		req.Header[http.CanonicalHeaderKey(k)] = append([]string(nil), vs...)
	}

	resp, err := c.httpClient.Do(req)
	if err != nil {
		return payload, nil, err
	}
	defer resp.Body.Close()

	if resp.StatusCode >= 300 {
		return payload, nil, transport.NewAPIErrorFromResponse(resp)
	}
	return payload, &transport.Response{Status: resp.StatusCode, Headers: resp.Header}, nil
}

// GetOptionalItems performs GET /optional-items. It returns the decoded body,
// the HTTP response metadata, and a *transport.APIError for non-2xx/3xx
// responses.
func (c *Client) GetOptionalItems(
	ctx context.Context,
	params *GetOptionalItemsParams,
	headers http.Header,
) (json.RawMessage, *transport.Response, error) {
	var payload json.RawMessage
	u := fmt.Sprintf("%s/optional-items", c.BaseURL)
	req, err := http.NewRequestWithContext(ctx, "GET", u, nil)
	if err != nil {
		return payload, nil, err
	}
	if params != nil {
		req.URL.RawQuery = params.toQuery().Encode()
	}
	for k, vs := range headers {
		req.Header[http.CanonicalHeaderKey(k)] = append([]string(nil), vs...)
	}

	resp, err := c.httpClient.Do(req)
	if err != nil {
		return payload, nil, err
	}
	defer resp.Body.Close()

	if resp.StatusCode >= 300 {
		return payload, nil, transport.NewAPIErrorFromResponse(resp)
	}
	return payload, &transport.Response{Status: resp.StatusCode, Headers: resp.Header}, nil
}
