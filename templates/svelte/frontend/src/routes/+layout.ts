// The app is a client-side SPA: sessions live in the browser and there is no
// server rendering, so disable SSR and prerendering for every route.
export const ssr = false;
export const prerender = false;
