/**
 * What the modal slot renders on every route that is not an interception,
 * which is nearly all of them. Without this file a refresh on any page would
 * 404 the slot.
 */
export default function Default() {
  return null;
}
