/**
 * The words the profile route uses, in one place.
 *
 * Like sign-in, the route renders twice: as a full page on a direct load, and
 * as a modal over whatever you were reading when it was intercepted.
 */
export const PROFILE_TITLE = 'Your profile';

export const PROFILE_DESCRIPTION =
  'Everything here runs through the backend: the avatar through a function and storage, the rest through auth and per-user GraphQL permissions.';
