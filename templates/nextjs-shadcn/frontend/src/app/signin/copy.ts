/**
 * The words the sign-in route uses, in one place.
 *
 * The route renders twice: as a full page on a direct load, and as a modal
 * over whatever you were reading when it was intercepted. Sharing the copy is
 * what stops the two renderings describing the same thing differently.
 */
export const SIGN_IN_TITLE = 'Sign in';

export const SIGN_IN_DESCRIPTION =
  'Enter your email and we will send you a one-time code. No account is needed — one is created the first time you sign in.';
