# Local backend state and secrets. `nhost init` also writes `backend/.gitignore`
# with these two entries; keeping them here means the rule ships with the
# template and survives that generated file being edited or removed.
.nhost
.secrets

# Dependencies installed outside frontend/, such as backend/functions.
node_modules/

# misc
.DS_Store
