{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next"]
    },
    "start": {
      "cache": false,
      "dependsOn": ["^build"]
    },
    "test": {
      "outputs": []
    },
    "docgen": {
      "outputs": ["docs/docs/**"]
    },
    "lint": { "outputs": [] },
    "lint:fix": { "outputs": [] }
  }
}
