{
  "baseBranch": "origin/main",
  "npmClient": "pnpm",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next"]
    },
    "start": {
      "dependsOn": ["^build"]
    },
    "test": {
      "outputs": []
    },
    "lint": { "outputs": [] },
    "lint:fix": { "outputs": [] }
  }
}
