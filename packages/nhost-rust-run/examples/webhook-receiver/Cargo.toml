[package]
name = "webhook-receiver"
version = "0.0.0-dev"
edition = "2021"
rust-version = "1.82"
publish = false

[dependencies]
nhost-run = { path = "../.." }
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
serde_json = "1"
