// Code generated by tools/codegen. DO NOT EDIT.
#![allow(
    dead_code,
    unused_imports,
    unused_mut,
    unused_variables,
    clippy::all,
    clippy::pedantic,
    clippy::nursery
)]

use crate::fetch::{ChainFunction, Error, FetchFn, FetchResponse, Response};
use serde::{Deserialize, Serialize};
use std::sync::Arc;

pub type Rfc2822Date = String;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ErrorResponseError {
    pub message: String,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub data: Option<serde_json::Value>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ErrorResponse {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub error: Option<ErrorResponseError>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ErrorResponseWithProcessedFilesError {
    pub message: String,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub data: Option<serde_json::Value>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ErrorResponseWithProcessedFiles {
    #[serde(
        rename = "processedFiles",
        skip_serializing_if = "Option::is_none",
        default
    )]
    pub processed_files: Option<Vec<FileMetadata>>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub error: Option<ErrorResponseWithProcessedFilesError>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct FileMetadata {
    pub id: String,
    pub name: String,
    pub size: i64,
    #[serde(rename = "bucketId")]
    pub bucket_id: String,
    pub etag: String,
    #[serde(rename = "createdAt")]
    pub created_at: String,
    #[serde(rename = "updatedAt")]
    pub updated_at: String,
    #[serde(rename = "isUploaded")]
    pub is_uploaded: bool,
    #[serde(rename = "mimeType")]
    pub mime_type: String,
    #[serde(
        rename = "uploadedByUserId",
        skip_serializing_if = "Option::is_none",
        default
    )]
    pub uploaded_by_user_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub metadata: Option<serde_json::Value>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct FileSummary {
    pub id: String,
    pub name: String,
    #[serde(rename = "bucketId")]
    pub bucket_id: String,
    #[serde(rename = "isUploaded")]
    pub is_uploaded: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PresignedUrlResponse {
    pub url: String,
    pub expiration: i64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateFileMetadata {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub metadata: Option<serde_json::Value>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UploadFileMetadata {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub metadata: Option<serde_json::Value>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct VersionInformation {
    #[serde(rename = "buildVersion")]
    pub build_version: String,
}

/// One of: "auto", "same", "jpeg", "webp", "png", "avif", "heic".
pub type OutputImageFormat = String;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UploadFilesBody {
    #[serde(rename = "bucket-id", skip_serializing_if = "Option::is_none", default)]
    pub bucket_id: Option<String>,
    #[serde(
        rename = "metadata[]",
        skip_serializing_if = "Option::is_none",
        default
    )]
    pub metadata: Option<Vec<UploadFileMetadata>>,
    #[serde(rename = "file[]")]
    pub file: Vec<Vec<u8>>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UploadFilesResponse201 {
    #[serde(rename = "processedFiles")]
    pub processed_files: Vec<FileMetadata>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ReplaceFileBody {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub metadata: Option<UpdateFileMetadata>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub file: Option<Vec<u8>>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteBrokenMetadataResponse200 {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub metadata: Option<Vec<FileSummary>>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteOrphanedFilesResponse200 {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub files: Option<Vec<String>>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListBrokenMetadataResponse200 {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub metadata: Option<Vec<FileSummary>>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListFilesNotUploadedResponse200 {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub metadata: Option<Vec<FileSummary>>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListOrphanedFilesResponse200 {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub files: Option<Vec<String>>,
}

#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetFileParams {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub q: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub h: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub w: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub b: Option<f64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub f: Option<OutputImageFormat>,
}

impl GetFileParams {
    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.q {
            q.push(("q".to_string(), v.to_string()));
        }
        if let Some(v) = &self.h {
            q.push(("h".to_string(), v.to_string()));
        }
        if let Some(v) = &self.w {
            q.push(("w".to_string(), v.to_string()));
        }
        if let Some(v) = &self.b {
            q.push(("b".to_string(), v.to_string()));
        }
        if let Some(v) = &self.f {
            q.push((
                "f".to_string(),
                serde_json::to_string(v).unwrap_or_default(),
            ));
        }
        q
    }
}

#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetFileMetadataHeadersParams {
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub q: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub h: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub w: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub b: Option<f64>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub f: Option<OutputImageFormat>,
}

impl GetFileMetadataHeadersParams {
    fn to_query(&self) -> Vec<(String, String)> {
        let mut q: Vec<(String, String)> = Vec::new();
        if let Some(v) = &self.q {
            q.push(("q".to_string(), v.to_string()));
        }
        if let Some(v) = &self.h {
            q.push(("h".to_string(), v.to_string()));
        }
        if let Some(v) = &self.w {
            q.push(("w".to_string(), v.to_string()));
        }
        if let Some(v) = &self.b {
            q.push(("b".to_string(), v.to_string()));
        }
        if let Some(v) = &self.f {
            q.push((
                "f".to_string(),
                serde_json::to_string(v).unwrap_or_default(),
            ));
        }
        q
    }
}

fn urlencode(s: &str) -> String {
    let mut out = String::with_capacity(s.len());
    for b in s.bytes() {
        match b {
            b'A'..=b'Z' | b'a'..=b'z' | b'0'..=b'9' | b'-' | b'_' | b'.' | b'~' => {
                out.push(b as char)
            }
            _ => out.push_str(&format!("%{b:02X}")),
        }
    }
    out
}

/// Generated API client backed by a reqwest::Client and a middleware chain.
pub struct Client {
    pub base_url: String,
    reqwest: reqwest::Client,
    chain_functions: Vec<ChainFunction>,
    fetch: Arc<dyn FetchFn>,
}

impl Client {
    /// Creates a new API client.
    pub fn new(
        base_url: String,
        chain_functions: Vec<ChainFunction>,
        reqwest: reqwest::Client,
    ) -> Self {
        let fetch = crate::fetch::create_enhanced_fetch(reqwest.clone(), &chain_functions);
        Self {
            base_url,
            reqwest,
            chain_functions,
            fetch,
        }
    }

    /// Appends a middleware chain function and rebuilds the pipeline.
    pub fn push_chain_function(&mut self, cf: ChainFunction) {
        self.chain_functions.push(cf);
        self.fetch =
            crate::fetch::create_enhanced_fetch(self.reqwest.clone(), &self.chain_functions);
    }

    /// Performs POST /files.
    pub async fn upload_files(
        &self,
        body: UploadFilesBody,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<UploadFilesResponse201>, Error> {
        let url = format!("{base}/files", base = self.base_url.as_str());
        let mut builder = self.reqwest.request(reqwest::Method::POST, &url);
        let mut form = reqwest::multipart::Form::new();
        if let Some(v) = &body.bucket_id {
            form = form.text("bucket-id", v.to_string());
        }
        if let Some(items) = &body.metadata {
            for (i, item) in items.iter().enumerate() {
                let _ = i;
                form = form.part(
                    "metadata[]",
                    reqwest::multipart::Part::text(serde_json::to_string(item).unwrap_or_default())
                        .mime_str("application/json")?,
                );
            }
        }
        {
            for (i, item) in body.file.iter().enumerate() {
                let _ = i;
                form = form.part(
                    "file[]",
                    reqwest::multipart::Part::bytes(item.clone()).file_name(format!("file-{i}")),
                );
            }
        }
        builder = builder.multipart(form);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: UploadFilesResponse201 = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs DELETE /files/{id}.
    pub async fn delete_file(
        &self,
        id: &str,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<serde_json::Value>, Error> {
        let url = format!("{base}/files/{id}", base = self.base_url.as_str(), id = id);
        let mut builder = self.reqwest.request(reqwest::Method::DELETE, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload = serde_json::Value::Null;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs GET /files/{id}.
    pub async fn get_file(
        &self,
        id: &str,
        params: Option<GetFileParams>,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<Vec<u8>>, Error> {
        let url = format!("{base}/files/{id}", base = self.base_url.as_str(), id = id);
        let mut builder = self.reqwest.request(reqwest::Method::GET, &url);
        if let Some(p) = &params {
            builder = builder.query(&p.to_query());
        }
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload = response.body.to_vec();
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs HEAD /files/{id}.
    pub async fn get_file_metadata_headers(
        &self,
        id: &str,
        params: Option<GetFileMetadataHeadersParams>,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<serde_json::Value>, Error> {
        let url = format!("{base}/files/{id}", base = self.base_url.as_str(), id = id);
        let mut builder = self.reqwest.request(reqwest::Method::HEAD, &url);
        if let Some(p) = &params {
            builder = builder.query(&p.to_query());
        }
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload = serde_json::Value::Null;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs PUT /files/{id}.
    pub async fn replace_file(
        &self,
        id: &str,
        body: ReplaceFileBody,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<FileMetadata>, Error> {
        let url = format!("{base}/files/{id}", base = self.base_url.as_str(), id = id);
        let mut builder = self.reqwest.request(reqwest::Method::PUT, &url);
        let mut form = reqwest::multipart::Form::new();
        form = form.part(
            "metadata",
            reqwest::multipart::Part::text(
                serde_json::to_string(&body.metadata).unwrap_or_default(),
            )
            .mime_str("application/json")?,
        );
        if let Some(v) = &body.file {
            form = form.part(
                "file",
                reqwest::multipart::Part::bytes(v.clone()).file_name("file"),
            );
        }
        builder = builder.multipart(form);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: FileMetadata = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs GET /files/{id}/presignedurl.
    pub async fn get_file_presigned_url(
        &self,
        id: &str,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<PresignedUrlResponse>, Error> {
        let url = format!(
            "{base}/files/{id}/presignedurl",
            base = self.base_url.as_str(),
            id = id
        );
        let mut builder = self.reqwest.request(reqwest::Method::GET, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: PresignedUrlResponse = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs POST /ops/delete-broken-metadata.
    pub async fn delete_broken_metadata(
        &self,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<DeleteBrokenMetadataResponse200>, Error> {
        let url = format!(
            "{base}/ops/delete-broken-metadata",
            base = self.base_url.as_str()
        );
        let mut builder = self.reqwest.request(reqwest::Method::POST, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: DeleteBrokenMetadataResponse200 = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs POST /ops/delete-orphans.
    pub async fn delete_orphaned_files(
        &self,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<DeleteOrphanedFilesResponse200>, Error> {
        let url = format!("{base}/ops/delete-orphans", base = self.base_url.as_str());
        let mut builder = self.reqwest.request(reqwest::Method::POST, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: DeleteOrphanedFilesResponse200 = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs POST /ops/list-broken-metadata.
    pub async fn list_broken_metadata(
        &self,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<ListBrokenMetadataResponse200>, Error> {
        let url = format!(
            "{base}/ops/list-broken-metadata",
            base = self.base_url.as_str()
        );
        let mut builder = self.reqwest.request(reqwest::Method::POST, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: ListBrokenMetadataResponse200 = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs POST /ops/list-not-uploaded.
    pub async fn list_files_not_uploaded(
        &self,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<ListFilesNotUploadedResponse200>, Error> {
        let url = format!(
            "{base}/ops/list-not-uploaded",
            base = self.base_url.as_str()
        );
        let mut builder = self.reqwest.request(reqwest::Method::POST, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: ListFilesNotUploadedResponse200 = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs POST /ops/list-orphans.
    pub async fn list_orphaned_files(
        &self,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<ListOrphanedFilesResponse200>, Error> {
        let url = format!("{base}/ops/list-orphans", base = self.base_url.as_str());
        let mut builder = self.reqwest.request(reqwest::Method::POST, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: ListOrphanedFilesResponse200 = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }

    /// Performs GET /version.
    pub async fn get_version(
        &self,
        headers: Option<reqwest::header::HeaderMap>,
    ) -> Result<FetchResponse<VersionInformation>, Error> {
        let url = format!("{base}/version", base = self.base_url.as_str());
        let mut builder = self.reqwest.request(reqwest::Method::GET, &url);
        if let Some(h) = headers {
            builder = builder.headers(h);
        }
        let request = builder.build()?;
        let response = self.fetch.call(request).await?;
        if response.status >= 300 {
            return Err(Error::from_response(response));
        }
        let payload: VersionInformation = serde_json::from_slice(&response.body)?;
        Ok(FetchResponse {
            body: payload,
            status: response.status,
            headers: response.headers,
        })
    }
}
