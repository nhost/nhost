[package]
name = "nhost-notes-cli"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# The Nhost SDK (native build: default rustls-tls backend, file-backed session).
nhost = { path = "../.." }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
serde_json = "1"
