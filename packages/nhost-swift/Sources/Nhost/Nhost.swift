import Foundation

/// Public package metadata for the Nhost Swift SDK runtime.
public enum NhostSDK: Sendable {
    /// Placeholder SDK version used until release automation wires package versions.
    public static let version = "0.0.0-dev"
}
