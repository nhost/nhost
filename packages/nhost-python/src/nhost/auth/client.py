# This file is auto-generated by tools/codegen. Do not edit manually.
from __future__ import annotations

import json
from collections.abc import Mapping, Sequence
from datetime import datetime
from typing import Any, Literal
from urllib.parse import quote
from uuid import UUID

import httpx
from pydantic import BaseModel, ConfigDict, Field

from ..fetch import (
    FetchResponse,
    HTTPError,
    Middleware,
    create_fetch_pipeline,
    decode_json,
    to_json,
    to_jsonable,
)

_MIN_ERROR_STATUS = 300


def _escape_path(value: object) -> str:
    segment = str(value)
    if segment == ".":
        return "%2E"
    if segment == "..":
        return "%2E%2E"
    return quote(segment, safe="")


def _parameter_scalar(value: Any) -> str:
    if value is None:
        return ""
    if isinstance(value, str):
        return value
    if isinstance(value, bool):
        return "true" if value else "false"
    if isinstance(value, (dict, list)):
        return json.dumps(value, separators=(",", ":"))
    return str(value)


def _query_parameter(name: str, value: Any, style: str, explode: bool) -> list[tuple[str, str]]:
    value = to_jsonable(value)
    if isinstance(value, list):
        if explode:
            return [(name, _parameter_scalar(item)) for item in value]
        return [(name, ",".join(_parameter_scalar(item) for item in value))]
    if isinstance(value, dict):
        if style == "deepObject":
            return [(f"{name}[{key}]", _parameter_scalar(item)) for key, item in value.items()]
        if explode:
            return [(key, _parameter_scalar(item)) for key, item in value.items()]
        parts = [part for key, item in value.items() for part in (key, _parameter_scalar(item))]
        return [(name, ",".join(parts))]
    return [(name, _parameter_scalar(value))]


AttestationFormat = Literal[
    "packed", "tpm", "android-key", "android-safetynet", "fido-u2f", "apple", "none"
]


class AuthenticationExtensionsClientOutputs(BaseModel):
    """Map of extension outputs from the client"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    appid: bool | None = Field(
        default=None,
        description="Application identifier extension output",
    )
    cred_props: CredentialPropertiesOutput | None = Field(
        default=None,
        alias="credProps",
        description="Credential properties extension output",
    )
    hmac_create_secret: bool | None = Field(
        default=None,
        alias="hmacCreateSecret",
        description="HMAC secret extension output",
    )


class AuthenticatorAssertionResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    client_data_json: str = Field(
        alias="clientDataJSON",
        description="Base64url encoded client data JSON",
    )
    authenticator_data: str = Field(
        alias="authenticatorData",
        description="Base64url encoded authenticator data",
    )
    signature: str = Field(
        repr=False,
        description="Base64url encoded assertion signature",
    )
    user_handle: str | None = Field(
        default=None,
        alias="userHandle",
        description="Base64url encoded user handle",
    )


AuthenticatorAttachment = Literal["platform", "cross-platform"]


class AuthenticatorAttestationResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    client_data_json: str = Field(
        alias="clientDataJSON",
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    transports: list[str] | None = Field(
        default=None,
        description="The authenticator transports",
    )
    authenticator_data: str | None = Field(
        default=None,
        alias="authenticatorData",
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    public_key: str | None = Field(
        default=None,
        alias="publicKey",
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    public_key_algorithm: int | None = Field(
        default=None,
        alias="publicKeyAlgorithm",
        description="The public key algorithm identifier\n\nFormat: int64",
    )
    attestation_object: str = Field(
        alias="attestationObject",
        description="Base64url-encoded binary data\n\nFormat: byte",
    )


class AuthenticatorSelection(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    authenticator_attachment: AuthenticatorAttachment | None = Field(
        default=None,
        alias="authenticatorAttachment",
        description="The authenticator attachment modality",
    )
    require_resident_key: bool | None = Field(
        default=None,
        alias="requireResidentKey",
        description=(
            "Whether the authenticator must create a client-side-resident public key credential "
            "source"
        ),
    )
    resident_key: ResidentKeyRequirement | None = Field(
        default=None,
        alias="residentKey",
        description="The resident key requirement",
    )
    user_verification: UserVerificationRequirement | None = Field(
        default=None,
        alias="userVerification",
        description="A requirement for user verification for the operation",
    )


AuthenticatorTransport = Literal["usb", "nfc", "ble", "smart-card", "hybrid", "internal"]

ConveyancePreference = Literal["none", "indirect", "direct", "enterprise"]


class CreatePATRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    expires_at: datetime = Field(
        alias="expiresAt",
        description="Expiration date of the PAT\n\nFormat: date-time",
    )
    metadata: dict[str, Any] | None = Field(
        default=None,
        description='Example: {"name":"my-pat","used-by":"my-app-cli"}',
    )


class CreatePATResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    id: str = Field(
        description=(
            'ID of the PAT\n\nExample: "2c35b6f3-c4b9-48e3-978a-d4d0f1d42e24"\n\nPattern: '
            "\\b[0-9a-f]{8}\\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\\b[0-9a-f]{12}\\b"
        ),
    )
    personal_access_token: str = Field(
        alias="personalAccessToken",
        repr=False,
        description=(
            'PAT\n\nExample: "2c35b6f3-c4b9-48e3-978a-d4d0f1d42e24"\n\nPattern: '
            "\\b[0-9a-f]{8}\\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\\b[0-9a-f]{12}\\b"
        ),
    )


class CredentialAssertionResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    id: str = Field(
        description="The credential's identifier",
    )
    type: str = Field(
        description="The credential type represented by this object",
    )
    raw_id: str = Field(
        alias="rawId",
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    client_extension_results: AuthenticationExtensionsClientOutputs | None = Field(
        default=None,
        alias="clientExtensionResults",
        description="Map of extension outputs from the client",
    )
    authenticator_attachment: str | None = Field(
        default=None,
        alias="authenticatorAttachment",
        description="The authenticator attachment",
    )
    response: AuthenticatorAssertionResponse


class CredentialCreationResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    id: str = Field(
        description="The credential's identifier",
    )
    type: str = Field(
        description="The credential type represented by this object",
    )
    raw_id: str = Field(
        alias="rawId",
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    client_extension_results: AuthenticationExtensionsClientOutputs | None = Field(
        default=None,
        alias="clientExtensionResults",
        description="Map of extension outputs from the client",
    )
    authenticator_attachment: str | None = Field(
        default=None,
        alias="authenticatorAttachment",
        description="The authenticator attachment",
    )
    response: AuthenticatorAttestationResponse


class CredentialParameter(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    type: CredentialType = Field(
        description="The valid credential types",
    )
    alg: int = Field(
        description="The cryptographic algorithm identifier",
    )


class CredentialPropertiesOutput(BaseModel):
    """Credential properties extension output"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    rk: bool | None = Field(
        default=None,
        description="Indicates if the credential is a resident key",
    )


CredentialType = Literal["public-key"]

ErrorResponseError = Literal[
    "default-role-must-be-in-allowed-roles",
    "disabled-endpoint",
    "disabled-user",
    "user-already-exists",
    "email-already-verified",
    "forbidden-anonymous",
    "internal-server-error",
    "invalid-email-password",
    "invalid-request",
    "locale-not-allowed",
    "password-too-short",
    "password-in-hibp-database",
    "redirectTo-not-allowed",
    "role-not-allowed",
    "signup-disabled",
    "unverified-user",
    "user-not-anonymous",
    "invalid-pat",
    "invalid-refresh-token",
    "invalid-ticket",
    "disabled-mfa-totp",
    "no-totp-secret",
    "invalid-totp",
    "mfa-type-not-found",
    "totp-already-active",
    "invalid-state",
    "oauth-token-echange-failed",
    "oauth-profile-fetch-failed",
    "oauth-provider-error",
    "invalid-otp",
    "otp-too-many-attempts",
    "cannot-send-sms",
    "provider-account-already-linked",
]


class ErrorResponse(BaseModel):
    """Standardized error response"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    status: int = Field(
        description="HTTP status error code\n\nExample: 400",
    )
    message: str = Field(
        description='Human-friendly error message\n\nExample: "Invalid email format"',
    )
    error: ErrorResponseError = Field(
        description="Error code identifying the specific application error",
    )


IdTokenProvider = Literal["apple", "google"]


class JWK(BaseModel):
    """JSON Web Key for JWT verification"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    alg: str = Field(
        description='Algorithm used with this key\n\nExample: "RS256"',
    )
    e: str = Field(
        description='RSA public exponent\n\nExample: "AQAB"',
    )
    kid: str = Field(
        description='Key ID\n\nExample: "key-id-1"',
    )
    kty: str = Field(
        description='Key type\n\nExample: "RSA"',
    )
    n: str = Field(
        description='RSA modulus\n\nExample: "abcd1234..."',
    )
    use: str = Field(
        description='Key usage\n\nExample: "sig"',
    )


class JWKSet(BaseModel):
    """JSON Web Key Set for verifying JWT signatures"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    keys: list[JWK] = Field(
        description="Array of public keys",
    )


class LinkIdTokenRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    provider: IdTokenProvider
    id_token: str = Field(
        alias="idToken",
        repr=False,
        description="Apple ID token",
    )
    nonce: str | None = Field(
        default=None,
        description="Nonce used during sign in process",
    )


class MFAChallengePayload(BaseModel):
    """Challenge payload for multi-factor authentication"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    ticket: str = Field(
        repr=False,
        description=(
            'Ticket to use when completing the MFA challenge\n\nExample: "mfaTotp:abc123def456"'
        ),
    )


OKResponse = Literal["OK"]


class OptionsRedirectTo(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    redirect_to: str | None = Field(
        default=None,
        alias="redirectTo",
        description='Example: "https://my-app.com/catch-redirection"\n\nFormat: uri',
    )


class PublicKeyCredentialCreationOptions(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    rp: RelyingPartyEntity
    user: UserEntity
    challenge: str = Field(
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    pub_key_cred_params: list[CredentialParameter] = Field(
        alias="pubKeyCredParams",
        description="The desired credential types and their respective cryptographic parameters",
    )
    timeout: int | None = Field(
        default=None,
        description=(
            "A time, in milliseconds, that the caller is willing to wait for the call to complete"
        ),
    )
    exclude_credentials: list[PublicKeyCredentialDescriptor] | None = Field(
        default=None,
        alias="excludeCredentials",
        description=(
            "A list of PublicKeyCredentialDescriptor objects representing public key credentials "
            "that are not acceptable to the caller"
        ),
    )
    authenticator_selection: AuthenticatorSelection | None = Field(
        default=None, alias="authenticatorSelection"
    )
    hints: list[PublicKeyCredentialHints] | None = Field(
        default=None,
        description="Hints to help guide the user through the experience",
    )
    attestation: ConveyancePreference | None = Field(
        default=None,
        description="The attestation conveyance preference",
    )
    attestation_formats: list[AttestationFormat] | None = Field(
        default=None,
        alias="attestationFormats",
        description="The preferred attestation statement formats",
    )
    extensions: dict[str, Any] | None = Field(
        default=None,
        description=(
            "Additional parameters requesting additional processing by the client and authenticator"
        ),
    )


class PublicKeyCredentialDescriptor(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    type: CredentialType = Field(
        description="The valid credential types",
    )
    id: str = Field(
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    transports: list[AuthenticatorTransport] | None = Field(
        default=None,
        description="The authenticator transports that can be used",
    )


PublicKeyCredentialHints = Literal["security-key", "client-device", "hybrid"]


class PublicKeyCredentialRequestOptions(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    challenge: str = Field(
        description="Base64url-encoded binary data\n\nFormat: byte",
    )
    timeout: int | None = Field(
        default=None,
        description=(
            "A time, in milliseconds, that the caller is willing to wait for the call to complete"
        ),
    )
    rp_id: str | None = Field(
        default=None,
        alias="rpId",
        description="The RP ID the credential should be scoped to",
    )
    allow_credentials: list[PublicKeyCredentialDescriptor] | None = Field(
        default=None,
        alias="allowCredentials",
        description=(
            "A list of CredentialDescriptor objects representing public key credentials acceptable "
            "to the caller"
        ),
    )
    user_verification: UserVerificationRequirement | None = Field(
        default=None,
        alias="userVerification",
        description="A requirement for user verification for the operation",
    )
    hints: list[PublicKeyCredentialHints] | None = Field(
        default=None,
        description="Hints to help guide the user through the experience",
    )
    extensions: dict[str, Any] | None = Field(
        default=None,
        description=(
            "Additional parameters requesting additional processing by the client and authenticator"
        ),
    )


class ProviderSession(BaseModel):
    """OAuth2 provider session containing access and refresh tokens"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    access_token: str = Field(
        alias="accessToken",
        repr=False,
        description='OAuth2 provider access token for API calls\n\nExample: "ya29.a0AfH6SMBx..."',
    )
    expires_in: int = Field(
        alias="expiresIn",
        description="Number of seconds until the access token expires\n\nExample: 3599",
    )
    expires_at: datetime = Field(
        alias="expiresAt",
        description=(
            'Timestamp when the access token expires\n\nExample: "2024-12-31T23:59:59Z"\n\nFormat: '
            "date-time"
        ),
    )
    refresh_token: str | None = Field(
        default=None,
        alias="refreshToken",
        repr=False,
        description=(
            "OAuth2 provider refresh token for obtaining new access tokens (if provided by the "
            'provider)\n\nExample: "1//0gK8..."'
        ),
    )


class ProviderSpecificParams(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    connection: str | None = Field(
        default=None,
        description="(workos) Specifies the connection to use for authentication",
    )
    organization: str | None = Field(
        default=None,
        description="(workos) Specifies the organization to use for authentication",
    )


class RefreshProviderTokenRequest(BaseModel):
    """Request to refresh OAuth2 provider tokens"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    refresh_token: str = Field(
        alias="refreshToken",
        repr=False,
        description=(
            "OAuth2 provider refresh token obtained from previous authentication\n\nExample: "
            '"1//0gK8..."'
        ),
    )


class RefreshTokenRequest(BaseModel):
    """Request to refresh an access token"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    refresh_token: str = Field(
        alias="refreshToken",
        repr=False,
        description=(
            "Refresh token used to generate a new access token\n\nExample: "
            '"2c35b6f3-c4b9-48e3-978a-d4d0f1d42e24"\n\nPattern: '
            "\\b[0-9a-f]{8}\\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\\b[0-9a-f]{12}\\b"
        ),
    )


class RelyingPartyEntity(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    name: str = Field(
        description="A human-palatable name for the entity",
    )
    id: str = Field(
        description="A unique identifier for the Relying Party entity, which sets the RP ID",
    )


ResidentKeyRequirement = Literal["discouraged", "preferred", "required"]


class Session(BaseModel):
    """User authentication session containing tokens and user information"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    access_token: str = Field(
        alias="accessToken",
        repr=False,
        description=(
            "JWT token for authenticating API requests\n\nExample: "
            '"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."'
        ),
    )
    access_token_expires_in: int = Field(
        alias="accessTokenExpiresIn",
        description=(
            "Expiration time of the access token in seconds\n\nExample: 900\n\nFormat: int64"
        ),
    )
    refresh_token_id: str = Field(
        alias="refreshTokenId",
        description=(
            'Identifier for the refresh token\n\nExample: "2c35b6f3-c4b9-48e3-978a-d4d0f1d42e24"\n'
            "\nPattern: \\b[0-9a-f]{8}\\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\\b[0-9a-f]{12}\\b"
        ),
    )
    refresh_token: str = Field(
        alias="refreshToken",
        repr=False,
        description=(
            "Token used to refresh the access token\n\nExample: "
            '"2c35b6f3-c4b9-48e3-978a-d4d0f1d42e24"\n\nPattern: '
            "\\b[0-9a-f]{8}\\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\\b[0-9a-f]{12}\\b"
        ),
    )
    user: User | None = Field(
        default=None,
        description="User profile and account information",
    )


class SessionPayload(BaseModel):
    """Container for session information"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    session: Session | None = Field(
        default=None,
        description="User authentication session containing tokens and user information",
    )


class SignInAnonymousRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    display_name: str | None = Field(
        default=None,
        alias="displayName",
        description='Example: "John Smith"',
    )
    locale: str | None = Field(
        default=None,
        description='A two or three characters locale\n\nExample: "en"',
    )
    metadata: dict[str, Any] | None = Field(
        default=None,
        description='Example: {"firstName":"John","lastName":"Smith"}',
    )


class SignInEmailPasswordRequest(BaseModel):
    """Request to authenticate using email and password"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='User\'s email address\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    password: str = Field(
        repr=False,
        description='User\'s password\n\nExample: "Str0ngPassw#ord-94|%"',
    )


class SignInEmailPasswordResponse(BaseModel):
    """Response for email-password authentication that may include a session or MFA challenge"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    session: Session | None = Field(
        default=None,
        description="User authentication session containing tokens and user information",
    )
    mfa: MFAChallengePayload | None = Field(
        default=None,
        description="Challenge payload for multi-factor authentication",
    )


class SignInIdTokenRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    provider: IdTokenProvider
    id_token: str = Field(
        alias="idToken",
        repr=False,
        description="Apple ID token",
    )
    nonce: str | None = Field(
        default=None,
        description="Nonce used during sign in process",
    )
    options: SignUpOptions | None = None


class SignInMfaTotpRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    ticket: str = Field(
        repr=False,
        description="Ticket\n\nPattern: ^mfaTotp:.*$",
    )
    otp: str = Field(
        repr=False,
        description="One time password",
    )


class SignInOTPEmailRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: SignUpOptions | None = None


class SignInOTPEmailVerifyRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    otp: str = Field(
        repr=False,
        description="One time password",
    )
    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )


class SignInOTPEmailVerifyResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    session: Session | None = Field(
        default=None,
        description="User authentication session containing tokens and user information",
    )


class SignInPATRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    personal_access_token: str = Field(
        alias="personalAccessToken",
        repr=False,
        description=(
            'PAT\n\nExample: "2c35b6f3-c4b9-48e3-978a-d4d0f1d42e24"\n\nPattern: '
            "\\b[0-9a-f]{8}\\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\\b[0-9a-f]{12}\\b"
        ),
    )


class SignInPasswordlessEmailRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: SignUpOptions | None = None
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the verification redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class SignInPasswordlessSmsOtpRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    phone_number: str = Field(
        alias="phoneNumber",
        description='Phone number of the user\n\nExample: "+123456789"',
    )
    otp: str = Field(
        repr=False,
        description="One-time password received by SMS",
    )


class SignInPasswordlessSmsOtpResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    session: Session | None = Field(
        default=None,
        description="User authentication session containing tokens and user information",
    )
    mfa: MFAChallengePayload | None = Field(
        default=None,
        description="Challenge payload for multi-factor authentication",
    )


class SignInPasswordlessSmsRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    phone_number: str = Field(
        alias="phoneNumber",
        description='Phone number of the user\n\nExample: "+123456789"',
    )
    options: SignUpOptions | None = None


class SignUpPasswordlessEmailRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: SignUpOptions | None = None
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the verification redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class SignUpOTPEmailRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: SignUpOptions | None = None


class SignUpPasswordlessSmsRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    phone_number: str = Field(
        alias="phoneNumber",
        description='Phone number of the user\n\nExample: "+123456789"',
    )
    options: SignUpOptions | None = None


class SignUpIdTokenRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    provider: IdTokenProvider
    id_token: str = Field(
        alias="idToken",
        repr=False,
        description="Apple or Google ID token",
    )
    nonce: str | None = Field(
        default=None,
        description="Nonce used during sign in process",
    )
    options: SignUpOptions | None = None


class SignInWebauthnRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str | None = Field(
        default=None,
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )


class SignInWebauthnVerifyRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str | None = Field(
        default=None,
        description=(
            'A valid email. Deprecated, no longer used\n\nExample: "john.smith@nhost.io"\n\n'
            "Format: email"
        ),
    )
    credential: CredentialAssertionResponse = Field(repr=False)


class SignOutRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    refresh_token: str | None = Field(
        default=None,
        alias="refreshToken",
        repr=False,
        description="Refresh token for the current session",
    )
    all: bool | None = Field(
        default=None,
        description="Sign out from all connected devices",
    )


class SignUpEmailPasswordRequest(BaseModel):
    """Request to register a new user with email and password"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description=(
            'Email address for the new user account\n\nExample: "john.smith@nhost.io"\n\nFormat: '
            "email"
        ),
    )
    password: str = Field(
        repr=False,
        description='Password for the new user account\n\nExample: "Str0ngPassw#ord-94|%"',
    )
    options: SignUpOptions | None = None
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided and email verification is required, the "
            "verification redirect will contain an authorization code instead of a refresh token.\n"
            "\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class SignUpOptions(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    allowed_roles: list[str] | None = Field(
        default=None,
        alias="allowedRoles",
        description='Example: ["me","user"]',
    )
    default_role: str | None = Field(
        default=None,
        alias="defaultRole",
        description='Example: "user"',
    )
    display_name: str | None = Field(
        default=None,
        alias="displayName",
        description='Example: "John Smith"\n\nPattern: ^[\\p{L}\\p{N}\\p{S} ,.\'-]+$',
    )
    locale: str | None = Field(
        default=None,
        description='A two or three characters locale\n\nExample: "en"',
    )
    metadata: dict[str, Any] | None = Field(
        default=None,
        description='Example: {"firstName":"John","lastName":"Smith"}',
    )
    redirect_to: str | None = Field(
        default=None,
        alias="redirectTo",
        description='Example: "https://my-app.com/catch-redirection"\n\nFormat: uri',
    )


class SignUpWebauthnRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: SignUpOptions | None = None


class SignUpWebauthnVerifyRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    credential: CredentialCreationResponse = Field(repr=False)
    options: SignUpOptions | None = None
    nickname: str | None = Field(
        default=None,
        description="Nickname for the security key",
    )
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided and email verification is required, the "
            "verification redirect will contain an authorization code instead of a refresh token.\n"
            "\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class TokenExchangeRequest(BaseModel):
    """Request to exchange an authorization code for a session using PKCE"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    code: str = Field(
        repr=False,
        description="The authorization code received from the redirect",
    )
    code_verifier: str = Field(
        alias="codeVerifier",
        repr=False,
        description="The original PKCE code verifier (43-128 characters)",
    )


class TotpGenerateResponse(BaseModel):
    """Response containing TOTP setup information for MFA"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    image_url: str = Field(
        alias="imageUrl",
        repr=False,
        description=(
            "URL to QR code image for scanning with an authenticator app\n\nExample: "
            '"data:image/png;base64,iVBORw0KGg..."'
        ),
    )
    totp_secret: str = Field(
        alias="totpSecret",
        repr=False,
        description=(
            "TOTP secret key for manual setup with an authenticator app\n\nExample: "
            '"ABCDEFGHIJK23456"'
        ),
    )


URLEncodedBase64 = str


class User(BaseModel):
    """User profile and account information"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    avatar_url: str = Field(
        alias="avatarUrl",
        description=(
            'URL to the user\'s profile picture\n\nExample: "https://myapp.com/avatars/user123.jpg"'
        ),
    )
    created_at: datetime = Field(
        alias="createdAt",
        description=(
            'Timestamp when the user account was created\n\nExample: "2023-01-15T12:34:56Z"\n\n'
            "Format: date-time"
        ),
    )
    default_role: str = Field(
        alias="defaultRole",
        description='Default authorization role for the user\n\nExample: "user"',
    )
    display_name: str = Field(
        alias="displayName",
        description='User\'s display name\n\nExample: "John Smith"',
    )
    email: str | None = Field(
        default=None,
        description='User\'s email address\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    email_verified: bool = Field(
        alias="emailVerified",
        description="Whether the user's email has been verified\n\nExample: true",
    )
    id: str = Field(
        description=(
            'Unique identifier for the user\n\nExample: "2c35b6f3-c4b9-48e3-978a-d4d0f1d42e24"\n\n'
            "Pattern: \\b[0-9a-f]{8}\\b-[0-9a-f]{4}-[0-9a-f]{4}-[0-9a-f]{4}-\\b[0-9a-f]{12}\\b"
        ),
    )
    is_anonymous: bool = Field(
        alias="isAnonymous",
        description="Whether this is an anonymous user account\n\nExample: false",
    )
    locale: str = Field(
        description='User\'s preferred locale (language code)\n\nExample: "en"',
    )
    metadata: dict[str, Any] | None = Field(
        description=(
            "Custom metadata associated with the user\n\nExample: "
            '{"firstName":"John","lastName":"Smith"}'
        ),
    )
    phone_number: str | None = Field(
        default=None,
        alias="phoneNumber",
        description='User\'s phone number\n\nExample: "+12025550123"',
    )
    phone_number_verified: bool = Field(
        alias="phoneNumberVerified",
        description="Whether the user's phone number has been verified\n\nExample: false",
    )
    roles: list[str] = Field(
        description='List of roles assigned to the user\n\nExample: ["user","customer"]',
    )
    active_mfa_type: str | None = Field(
        default=None,
        alias="activeMfaType",
        description="Active MFA type for the user",
    )


UserDeanonymizeRequestSignInMethod = Literal["email-password", "passwordless"]


class UserDeanonymizeRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    sign_in_method: UserDeanonymizeRequestSignInMethod = Field(
        alias="signInMethod",
        description="Which sign-in method to use",
    )
    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    password: str | None = Field(
        default=None,
        repr=False,
        description='A password of minimum 3 characters\n\nExample: "Str0ngPassw#ord-94|%"',
    )
    connection: str | None = Field(
        default=None,
        description="Deprecated, will be ignored",
    )
    options: SignUpOptions | None = None
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the verification redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class UserDeanonymizeSmsRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    phone_number: str = Field(
        alias="phoneNumber",
        description='Phone number of the user\n\nExample: "+123456789"',
    )
    options: SignUpOptions | None = None


class UserEmailChangeRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    new_email: str = Field(
        alias="newEmail",
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: OptionsRedirectTo | None = None
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the verification redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class UserPhoneNumberChangeRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    new_phone_number: str = Field(
        alias="newPhoneNumber",
        description=(
            "New phone number to bind to the user once verified via SMS OTP\n\nExample: "
            '"+123456789"'
        ),
    )


class UserPhoneNumberChangeVerifyRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    new_phone_number: str = Field(
        alias="newPhoneNumber",
        description=(
            "The phone number that was previously requested via /user/phone-number/change\n\n"
            'Example: "+123456789"'
        ),
    )
    otp: str = Field(
        repr=False,
        description=(
            'One-time password received via SMS at the new phone number\n\nExample: "123456"'
        ),
    )


class UserEmailSendVerificationEmailRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: OptionsRedirectTo | None = None
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the verification redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class UserEntity(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    name: str = Field(
        description="A human-palatable name for the entity",
    )
    display_name: str = Field(
        alias="displayName",
        description="A human-palatable name for the user account, intended only for display",
    )
    id: str = Field(
        description="The user handle of the user account entity",
    )


UserMfaRequestActiveMfaType = Literal["totp", ""]


class UserMfaRequest(BaseModel):
    """Request to activate or deactivate multi-factor authentication"""

    model_config = ConfigDict(populate_by_name=True, extra="allow")

    code: str = Field(
        repr=False,
        description=(
            'Verification code from the authenticator app when activating MFA\n\nExample: "123456"'
        ),
    )
    active_mfa_type: UserMfaRequestActiveMfaType | None = Field(
        default=None,
        alias="activeMfaType",
        description='Type of MFA to activate. Use empty string to disable MFA.\n\nExample: "totp"',
    )


class UserPasswordRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    new_password: str = Field(
        alias="newPassword",
        repr=False,
        description='A password of minimum 3 characters\n\nExample: "Str0ngPassw#ord-94|%"',
    )
    ticket: str | None = Field(
        default=None,
        repr=False,
        description=(
            "Ticket to reset the password, required if the user is not authenticated\n\nPattern: "
            "^passwordReset\\:.*$"
        ),
    )


class UserPasswordResetRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    email: str = Field(
        description='A valid email\n\nExample: "john.smith@nhost.io"\n\nFormat: email',
    )
    options: OptionsRedirectTo | None = None
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the verification redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


UserVerificationRequirement = Literal["required", "preferred", "discouraged"]


class VerifyAddSecurityKeyRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    credential: CredentialCreationResponse = Field(repr=False)
    nickname: str | None = Field(
        default=None,
        description="Optional nickname for the security key",
    )


class VerifyAddSecurityKeyResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    id: str = Field(
        description=(
            "The ID of the newly added security key\n\nExample: "
            '"123e4567-e89b-12d3-a456-426614174000"'
        ),
    )
    nickname: str | None = Field(
        default=None,
        description="The nickname of the security key if provided",
    )


class VerifyTokenRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    token: str | None = Field(
        default=None,
        repr=False,
        description="JWT token to verify",
    )


class OAuth2ErrorResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    error: str = Field(
        description="OAuth2 error code",
    )
    error_description: str | None = Field(
        default=None,
        description="Human-readable error description",
    )


class OAuth2DiscoveryResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    issuer: str
    authorization_endpoint: str
    token_endpoint: str
    userinfo_endpoint: str | None = None
    jwks_uri: str
    revocation_endpoint: str | None = None
    introspection_endpoint: str | None = None
    scopes_supported: list[str] | None = None
    response_types_supported: list[str]
    grant_types_supported: list[str] | None = None
    subject_types_supported: list[str] | None = None
    id_token_signing_alg_values_supported: list[str] | None = None
    token_endpoint_auth_methods_supported: list[str] | None = None
    code_challenge_methods_supported: list[str] | None = None
    claims_supported: list[str] | None = None
    request_parameter_supported: bool | None = None
    authorization_response_iss_parameter_supported: bool | None = None
    client_id_metadata_document_supported: bool | None = None


OAuth2TokenRequestGrantType = Literal["authorization_code", "refresh_token"]


class OAuth2TokenRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    grant_type: OAuth2TokenRequestGrantType
    code: str | None = Field(default=None, repr=False)
    redirect_uri: str | None = None
    client_id: str | None = None
    client_secret: str | None = Field(default=None, repr=False)
    code_verifier: str | None = Field(default=None, repr=False)
    refresh_token: str | None = Field(default=None, repr=False)
    resource: str | None = None


class OAuth2TokenResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    access_token: str = Field(repr=False)
    token_type: str
    expires_in: int
    refresh_token: str | None = Field(default=None, repr=False)
    id_token: str | None = Field(default=None, repr=False)
    scope: str | None = None


class OAuth2UserinfoResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    sub: str
    name: str | None = None
    email: str | None = None
    email_verified: bool | None = None
    picture: str | None = None
    locale: str | None = None
    phone_number: str | None = None
    phone_number_verified: bool | None = None


class OAuth2JWKSResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    keys: list[JWK]


OAuth2RevokeRequestTokenTypeHint = Literal["access_token", "refresh_token"]


class OAuth2RevokeRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    token: str = Field(repr=False)
    token_type_hint: OAuth2RevokeRequestTokenTypeHint | None = None
    client_id: str | None = None
    client_secret: str | None = Field(default=None, repr=False)


OAuth2IntrospectRequestTokenTypeHint = Literal["access_token", "refresh_token"]


class OAuth2IntrospectRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    token: str = Field(repr=False)
    token_type_hint: OAuth2IntrospectRequestTokenTypeHint | None = None
    client_id: str | None = None
    client_secret: str | None = Field(default=None, repr=False)


class OAuth2IntrospectResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    active: bool
    scope: str | None = None
    client_id: str | None = None
    sub: str | None = None
    exp: int | None = None
    iat: int | None = None
    iss: str | None = None
    token_type: str | None = None


class OAuth2LoginResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    request_id: UUID = Field(
        alias="requestId",
        description="Format: uuid",
    )
    client_id: str = Field(alias="clientId")
    scopes: list[str]
    redirect_uri: str = Field(alias="redirectUri")


class OAuth2LoginRequest(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    request_id: UUID = Field(
        alias="requestId",
        description="Format: uuid",
    )


class OAuth2LoginCompleteResponse(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    redirect_uri: str = Field(
        alias="redirectUri",
        description="Format: uri",
    )


RedirectToQuery = str

SignInProvider = Literal[
    "apple",
    "github",
    "google",
    "linkedin",
    "discord",
    "spotify",
    "twitch",
    "gitlab",
    "bitbucket",
    "workos",
    "azuread",
    "entraid",
    "strava",
    "facebook",
    "windowslive",
    "twitter",
]

TicketQuery = str

TicketTypeQuery = Literal[
    "emailVerify", "emailConfirmChange", "signinPasswordless", "passwordReset"
]


class GetVersionResponse200(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    version: str = Field(
        description='The version of the authentication service\n\nExample: "1.2.3"',
    )


GetCodeChallengeMethod = Literal["S256"]


class Oauth2AuthorizePostBody(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="allow")

    client_id: str
    redirect_uri: str
    response_type: str
    scope: str | None = None
    state: str | None = None
    nonce: str | None = None
    code_challenge: str | None = None
    code_challenge_method: str | None = Field(
        default=None,
        description="Only S256 is supported. The plain method is not allowed.",
    )
    resource: str | None = None
    prompt: str | None = None


class SignInProviderParams(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="forbid")

    allowed_roles: list[str] | None = Field(
        default=None,
        alias="allowedRoles",
        description='Array of allowed roles for the user\n\nExample: ["me","user"]',
    )
    default_role: str | None = Field(
        default=None,
        alias="defaultRole",
        description='Default role for the user\n\nExample: "user"',
    )
    display_name: str | None = Field(
        default=None,
        alias="displayName",
        description=(
            'Display name for the user\n\nExample: "John Smith"\n\nPattern: ^[\\p{L}\\p{N}\\p{S} '
            ",.'-]+$"
        ),
    )
    locale: str | None = Field(
        default=None,
        description='A two or three characters locale\n\nExample: "en"',
    )
    metadata: dict[str, Any] | None = Field(
        default=None,
        description=(
            "Additional metadata for the user (JSON encoded string)\n\nExample: "
            '{"firstName":"John","lastName":"Smith"}'
        ),
    )
    redirect_to: str | None = Field(
        default=None,
        alias="redirectTo",
        description=(
            'URI to redirect to\n\nExample: "https://my-app.com/catch-redirection"\n\nFormat: uri'
        ),
    )
    connect: str | None = Field(
        default=None,
        description=(
            "If set, this means that the user is already authenticated and wants to link their "
            "account. This needs to be a valid JWT access token."
        ),
    )
    state: str | None = Field(
        default=None,
        description="Opaque state value to be returned by the provider",
    )
    provider_specific_params: ProviderSpecificParams | None = Field(
        default=None,
        alias="providerSpecificParams",
        description="Additional provider-specific parameters",
    )
    upstream_params: dict[str, Any] | None = Field(
        default=None,
        alias="upstreamParams",
        description=(
            "Extra parameters forwarded to the upstream OAuth2 provider's authorization URL. "
            "Reserved OAuth2/OIDC parameters are rejected.\n\nExtra parameters forwarded to the "
            "upstream OAuth2 provider's authorization URL (e.g. Google's prompt or login_hint). "
            "Reserved OAuth2/OIDC parameters are rejected.\n"
        ),
    )
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the callback redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class SignUpProviderParams(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="forbid")

    allowed_roles: list[str] | None = Field(
        default=None,
        alias="allowedRoles",
        description='Array of allowed roles for the user\n\nExample: ["me","user"]',
    )
    default_role: str | None = Field(
        default=None,
        alias="defaultRole",
        description='Default role for the user\n\nExample: "user"',
    )
    display_name: str | None = Field(
        default=None,
        alias="displayName",
        description=(
            'Display name for the user\n\nExample: "John Smith"\n\nPattern: ^[\\p{L}\\p{N}\\p{S} '
            ",.'-]+$"
        ),
    )
    locale: str | None = Field(
        default=None,
        description='A two or three characters locale\n\nExample: "en"',
    )
    metadata: dict[str, Any] | None = Field(
        default=None,
        description=(
            "Additional metadata for the user (JSON encoded string)\n\nExample: "
            '{"firstName":"John","lastName":"Smith"}'
        ),
    )
    redirect_to: str | None = Field(
        default=None,
        alias="redirectTo",
        description=(
            'URI to redirect to\n\nExample: "https://my-app.com/catch-redirection"\n\nFormat: uri'
        ),
    )
    state: str | None = Field(
        default=None,
        description="Opaque state value to be returned by the provider",
    )
    provider_specific_params: ProviderSpecificParams | None = Field(
        default=None,
        alias="providerSpecificParams",
        description="Additional provider-specific parameters",
    )
    upstream_params: dict[str, Any] | None = Field(
        default=None,
        alias="upstreamParams",
        description=(
            "Extra parameters forwarded to the upstream OAuth2 provider's authorization URL. "
            "Reserved OAuth2/OIDC parameters are rejected.\n\nExtra parameters forwarded to the "
            "upstream OAuth2 provider's authorization URL (e.g. Google's prompt or login_hint). "
            "Reserved OAuth2/OIDC parameters are rejected.\n"
        ),
    )
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When provided, the callback redirect will contain an "
            "authorization code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class VerifyTicketParams(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="forbid")

    ticket: TicketQuery = Field(
        repr=False,
        description='Ticket\n\nExample: "verifyEmail:xxxxxxxx"',
    )
    type: TicketTypeQuery | None = Field(
        default=None,
        description=(
            "Type of the ticket. Deprecated, no longer used\n\nType of the ticket\n\nExample: "
            '"emailVerify"'
        ),
    )
    redirect_to: RedirectToQuery = Field(
        alias="redirectTo",
        description=(
            'Target URL for the redirect\n\nExample: "https://my-app.com/catch-redirection"\n\n'
            "Format: uri"
        ),
    )
    code_challenge: str | None = Field(
        default=None,
        alias="codeChallenge",
        description=(
            "PKCE code challenge (S256). When present, the redirect will contain an authorization "
            "code instead of a refresh token.\n\nPattern: ^[A-Za-z0-9_-]{43}$"
        ),
    )


class Oauth2AuthorizeParams(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="forbid")

    client_id: str = Field(
        description="The OAuth2 client identifier (RFC 6749 Section 2.2).",
    )
    redirect_uri: str = Field(
        description=(
            "The URI to redirect the user-agent to after authorization (RFC 6749 Section 3.1.2)."
        ),
    )
    response_type: str = Field(
        description=(
            "The authorization response type. Only 'code' is supported (RFC 6749 Section 3.1.1)."
        ),
    )
    scope: str | None = Field(
        default=None,
        description="Space-delimited list of requested scopes (RFC 6749 Section 3.3).",
    )
    state: str | None = Field(
        default=None,
        description=(
            "Opaque value used to maintain state between the request and callback (RFC 6749 "
            "Section 4.1.1)."
        ),
    )
    nonce: str | None = Field(
        default=None,
        description=(
            "String value used to associate a client session with an ID token (OpenID Connect Core "
            "Section 3.1.2.1)."
        ),
    )
    code_challenge: str | None = Field(
        default=None,
        description="PKCE code challenge derived from the code verifier (RFC 7636 Section 4.2).",
    )
    code_challenge_method: GetCodeChallengeMethod | None = Field(
        default=None,
        description="Only S256 is supported. The plain method is not allowed.",
    )
    resource: str | None = Field(
        default=None,
        description="Resource indicator for the target service (RFC 8707).",
    )
    prompt: str | None = Field(
        default=None,
        description=(
            "Space-delimited list of prompts to present to the user (OpenID Connect Core Section "
            "3.1.2.1)."
        ),
    )


class Oauth2LoginGetParams(BaseModel):
    model_config = ConfigDict(populate_by_name=True, extra="forbid")

    request_id: UUID = Field(
        description="The pending authorization request identifier.\n\nFormat: uuid",
    )


class Client:
    """Generated async API client backed by an httpx.AsyncClient and a middleware chain."""

    def __init__(
        self,
        base_url: str,
        *,
        middleware: Sequence[Middleware] = (),
        http_client: httpx.AsyncClient | None = None,
    ) -> None:
        self.base_url = base_url
        self._middleware = list(middleware)
        self._owns_http_client = http_client is None
        self._http = http_client if http_client is not None else httpx.AsyncClient()
        self._fetch = create_fetch_pipeline(self._http, self._middleware)

    async def __aenter__(self) -> Client:
        return self

    async def __aexit__(self, *_: object) -> None:
        await self.aclose()

    async def aclose(self) -> None:
        """Close the internally owned HTTP client, if any."""
        if self._owns_http_client:
            await self._http.aclose()

    def add_middleware(self, middleware: Middleware) -> None:
        """Append HTTP middleware and rebuild the request pipeline."""
        self._middleware.append(middleware)
        self._fetch = create_fetch_pipeline(self._http, self._middleware)

    async def get_jw_ks(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[JWKSet]:
        (
            """Get public keys for JWT verification in JWK Set format\n\nRetrieve the JSON Web """
            """Key Set (JWKS) containing public keys used to verify JWT signatures. This """
            """endpoint is used by clients to validate access tokens.\n\nArgs:\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[JWKSet]: The HTTP response."""
        )
        url = f"{self.base_url}/.well-known/jwks.json"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, JWKSet)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def elevate_webauthn(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[PublicKeyCredentialRequestOptions]:
        (
            """Elevate access for an already signed in user using FIDO2 Webauthn\n\nGenerate a """
            """Webauthn challenge for elevating user permissions\n\nArgs:\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[PublicKeyCredentialRequestOptions]: The HTTP response."""
        )
        url = f"{self.base_url}/elevate/webauthn"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, PublicKeyCredentialRequestOptions)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_elevate_webauthn(
        self,
        *,
        body: SignInWebauthnVerifyRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Verify FIDO2 Webauthn authentication using public-key cryptography for elevation\n"""
            """\nComplete Webauthn elevation by verifying the authentication response\n\nArgs:\n """
            """   body (SignInWebauthnVerifyRequest): Request body.\n    headers (Mapping[str, """
            """str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/elevate/webauthn/verify"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def health_check_get(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Health check (GET)\n\nVerify if the authentication service is operational using """
            """GET method\n\nArgs:\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/healthz"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def health_check_head(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[None]:
        (
            """Health check (HEAD)\n\nVerify if the authentication service is operational using """
            """HEAD method\n\nArgs:\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[None]: The HTTP response."""
        )
        url = f"{self.base_url}/healthz"
        query = None
        request = self._http.build_request(
            "HEAD",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = None
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def link_id_token(
        self,
        *,
        body: LinkIdTokenRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Link a user account with the provider's account using an id token\n\nLink the """
            """authenticated user's account with an external OAuth provider account using an ID """
            """token. Requires elevated permissions.\n\nArgs:\n    body (LinkIdTokenRequest): """
            """Request body.\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/link/idtoken"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def change_user_mfa(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[TotpGenerateResponse]:
        (
            """Generate TOTP secret\n\nGenerate a Time-based One-Time Password (TOTP) secret for """
            """setting up multi-factor authentication\n\nArgs:\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[TotpGenerateResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/mfa/totp/generate"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, TotpGenerateResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def create_pat(
        self,
        *,
        body: CreatePATRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[CreatePATResponse]:
        (
            """Create a Personal Access Token (PAT)\n\nGenerate a new Personal Access Token for """
            """programmatic API access. PATs are long-lived tokens that can be used instead of """
            """regular authentication for automated systems. Requires elevated permissions.\n\n"""
            """Args:\n    body (CreatePATRequest): Request body.\n    headers (Mapping[str, str] """
            """| None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[CreatePATResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/pat"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, CreatePATResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_anonymous(
        self,
        *,
        body: SignInAnonymousRequest | None = None,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Sign in anonymously\n\nCreate an anonymous user session without providing """
            """credentials. Anonymous users can be converted to regular users later via the """
            """deanonymize endpoint.\nThis endpoint always creates a new user and is **not** """
            """gated by `AUTH_DISABLE_AUTO_SIGNUP`; it is controlled by `AUTH_DISABLE_SIGNUP` """
            """and `AUTH_ANONYMOUS_USERS_ENABLED`.\n\n\nArgs:\n    body """
            """(SignInAnonymousRequest): Request body.\n    headers (Mapping[str, str] | None): """
            """Additional request headers.\n\nReturns:\n    FetchResponse[SessionPayload]: The """
            """HTTP response."""
        )
        url = f"{self.base_url}/signin/anonymous"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_email_password(
        self,
        *,
        body: SignInEmailPasswordRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SignInEmailPasswordResponse]:
        (
            """Sign in with email and password\n\nAuthenticate a user with their email and """
            """password. Returns a session object or MFA challenge if two-factor authentication """
            """is enabled.\n\nArgs:\n    body (SignInEmailPasswordRequest): Request body.\n    """
            """headers (Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SignInEmailPasswordResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/email-password"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SignInEmailPasswordResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_id_token(
        self,
        *,
        body: SignInIdTokenRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Sign in with an ID token\n\nAuthenticate using an ID token from a supported OAuth """
            """provider (Apple or Google).\nIf the user doesn't exist and """
            """`AUTH_DISABLE_AUTO_SIGNUP` is not set, a new account will be created.\nWhen """
            """`AUTH_DISABLE_AUTO_SIGNUP` is enabled, users must use the `/signup/idtoken` """
            """endpoint to register first.\n\n\nArgs:\n    body (SignInIdTokenRequest): Request """
            """body.\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/idtoken"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_sign_in_mfa_totp(
        self,
        *,
        body: SignInMfaTotpRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Verify TOTP for MFA\n\nComplete the multi-factor authentication by verifying a """
            """Time-based One-Time Password (TOTP). Returns a session if validation is """
            """successful.\n\nArgs:\n    body (SignInMfaTotpRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/mfa/totp"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_otp_email(
        self,
        *,
        body: SignInOTPEmailRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Sign in with email OTP\n\nInitiate email-based one-time password authentication. """
            """Sends an OTP to the specified email address.\nIf the user doesn't exist and """
            """`AUTH_DISABLE_AUTO_SIGNUP` is not set, a new account will be created with the """
            """provided options.\nWhen `AUTH_DISABLE_AUTO_SIGNUP` is enabled, users must use the """
            """`/signup/otp/email` endpoint to register first.\n\n\nArgs:\n    body """
            """(SignInOTPEmailRequest): Request body.\n    headers (Mapping[str, str] | None): """
            """Additional request headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP """
            """response."""
        )
        url = f"{self.base_url}/signin/otp/email"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_sign_in_otp_email(
        self,
        *,
        body: SignInOTPEmailVerifyRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SignInOTPEmailVerifyResponse]:
        (
            """Verify email OTP\n\nComplete email OTP authentication by verifying the one-time """
            """password. Returns a session if validation is successful.\n\nArgs:\n    body """
            """(SignInOTPEmailVerifyRequest): Request body.\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SignInOTPEmailVerifyResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/otp/email/verify"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SignInOTPEmailVerifyResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_passwordless_email(
        self,
        *,
        body: SignInPasswordlessEmailRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Sign in with magic link email\n\nInitiate passwordless authentication by sending """
            """a magic link to the user's email.\nIf the user doesn't exist and """
            """`AUTH_DISABLE_AUTO_SIGNUP` is not set, a new account will be created with the """
            """provided options.\nWhen `AUTH_DISABLE_AUTO_SIGNUP` is enabled, users must use the """
            """`/signup/passwordless/email` endpoint to register first.\n\n\nArgs:\n    body """
            """(SignInPasswordlessEmailRequest): Request body.\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    FetchResponse[OKResponse]: """
            """The HTTP response."""
        )
        url = f"{self.base_url}/signin/passwordless/email"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_passwordless_sms(
        self,
        *,
        body: SignInPasswordlessSmsRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Sign in with SMS OTP\n\nInitiate passwordless authentication by sending a """
            """one-time password to the user's phone number.\nIf the user doesn't exist and """
            """`AUTH_DISABLE_AUTO_SIGNUP` is not set, a new account will be created with the """
            """provided options.\nWhen `AUTH_DISABLE_AUTO_SIGNUP` is enabled, users must use the """
            """`/signup/passwordless/sms` endpoint to register first.\n\n\nArgs:\n    body """
            """(SignInPasswordlessSmsRequest): Request body.\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    FetchResponse[OKResponse]: """
            """The HTTP response."""
        )
        url = f"{self.base_url}/signin/passwordless/sms"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_sign_in_passwordless_sms(
        self,
        *,
        body: SignInPasswordlessSmsOtpRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SignInPasswordlessSmsOtpResponse]:
        (
            """Verify SMS OTP and complete authentication\n\nComplete passwordless SMS """
            """authentication by verifying the one-time password and returning a session.\n\n"""
            """Args:\n    body (SignInPasswordlessSmsOtpRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SignInPasswordlessSmsOtpResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/passwordless/sms/otp"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SignInPasswordlessSmsOtpResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_pat(
        self,
        *,
        body: SignInPATRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Sign in with Personal Access Token (PAT)\n\nAuthenticate using a Personal Access """
            """Token. PATs are long-lived tokens that can be used for programmatic access to the """
            """API.\n\nArgs:\n    body (SignInPATRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/pat"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    def sign_in_provider_url(
        self,
        provider: SignInProvider,
        *,
        params: SignInProviderParams | None = None,
    ) -> str:
        (
            """Sign in with an OAuth2 provider\n\nInitiate OAuth2 authentication flow with a """
            """social provider. Redirects the user to the provider's authorization page.\nIf the """
            """user doesn't exist and `AUTH_DISABLE_AUTO_SIGNUP` is not set, a new account will """
            """be created upon callback.\nWhen `AUTH_DISABLE_AUTO_SIGNUP` is enabled, users must """
            """use the `/signup/provider/{provider}` endpoint to register first.\n\n\nArgs:\n    """
            """provider (SignInProvider): The name of the social provider\n    params """
            """(SignInProviderParams): Query and header parameters.\n\nReturns:\n    str: The """
            """redirect URL."""
        )
        url = f"{self.base_url}/signin/provider/{_escape_path(provider)}"

        query: list[tuple[str, str | int | float | bool | None]] = []
        if params is not None and params.allowed_roles is not None:
            query.extend(_query_parameter("allowedRoles", params.allowed_roles, "form", False))
        if params is not None and params.default_role is not None:
            query.extend(_query_parameter("defaultRole", params.default_role, "form", True))
        if params is not None and params.display_name is not None:
            query.extend(_query_parameter("displayName", params.display_name, "form", True))
        if params is not None and params.locale is not None:
            query.extend(_query_parameter("locale", params.locale, "form", True))
        if params is not None and params.metadata is not None:
            query.append(("metadata", to_json(params.metadata)))
        if params is not None and params.redirect_to is not None:
            query.extend(_query_parameter("redirectTo", params.redirect_to, "form", True))
        if params is not None and params.connect is not None:
            query.extend(_query_parameter("connect", params.connect, "form", True))
        if params is not None and params.state is not None:
            query.extend(_query_parameter("state", params.state, "form", True))
        if params is not None and params.provider_specific_params is not None:
            query.extend(
                _query_parameter(
                    "providerSpecificParams", params.provider_specific_params, "form", True
                )
            )
        if params is not None and params.upstream_params is not None:
            query.extend(
                _query_parameter("upstreamParams", params.upstream_params, "deepObject", True)
            )
        if params is not None and params.code_challenge is not None:
            query.extend(_query_parameter("codeChallenge", params.code_challenge, "form", True))
        if query:
            return str(self._http.build_request("GET", url, params=query).url)
        return url

    async def get_provider_tokens(
        self,
        provider: SignInProvider,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[ProviderSession]:
        (
            """Retrieve OAuth2 provider tokens from callback\n\nAfter successful OAuth2 """
            """authentication, retrieve the provider session containing access token, refresh """
            """token, and expiration information for the specified provider. To ensure the data """
            """isn't stale this endpoint must be called immediately after the OAuth callback to """
            """obtain the tokens. The session is cleared from the database during this call, so """
            """subsequent calls will fail without going through the sign-in flow again. It is """
            """the user's responsibility to store the session safely (e.g., in browser local """
            """storage).\n\nArgs:\n    provider (SignInProvider): The name of the social """
            """provider\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[ProviderSession]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/provider/{_escape_path(provider)}/callback/tokens"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, ProviderSession)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_in_webauthn(
        self,
        *,
        body: SignInWebauthnRequest | None = None,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[PublicKeyCredentialRequestOptions]:
        (
            """Sign in with Webauthn\n\nInitiate a Webauthn sign-in process by sending a """
            """challenge to the user's device. The user must have previously registered a """
            """Webauthn credential.\n\nArgs:\n    body (SignInWebauthnRequest): Request body.\n  """
            """  headers (Mapping[str, str] | None): Additional request headers.\n\nReturns:\n   """
            """ FetchResponse[PublicKeyCredentialRequestOptions]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/webauthn"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, PublicKeyCredentialRequestOptions)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_sign_in_webauthn(
        self,
        *,
        body: SignInWebauthnVerifyRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Verify Webauthn sign-in\n\nComplete the Webauthn sign-in process by verifying the """
            """response from the user's device. Returns a session if validation is successful.\n"""
            """\nArgs:\n    body (SignInWebauthnVerifyRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/signin/webauthn/verify"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_out(
        self,
        *,
        body: SignOutRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Sign out\n\nEnd the current user session by invalidating refresh tokens. """
            """Optionally sign out from all devices.\n\nArgs:\n    body (SignOutRequest): """
            """Request body.\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/signout"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_up_email_password(
        self,
        *,
        body: SignUpEmailPasswordRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Sign up with email and password\n\nRegister a new user account with email and """
            """password. Returns a session if email verification is not required, otherwise """
            """returns null session.\n\nArgs:\n    body (SignUpEmailPasswordRequest): Request """
            """body.\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/signup/email-password"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_up_webauthn(
        self,
        *,
        body: SignUpWebauthnRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[PublicKeyCredentialCreationOptions]:
        (
            """Sign up with Webauthn\n\nInitiate a Webauthn sign-up process by sending a """
            """challenge to the user's device. The user must not have an existing account.\n\n"""
            """Args:\n    body (SignUpWebauthnRequest): Request body.\n    headers (Mapping[str, """
            """str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[PublicKeyCredentialCreationOptions]: The HTTP response."""
        )
        url = f"{self.base_url}/signup/webauthn"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, PublicKeyCredentialCreationOptions)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_sign_up_webauthn(
        self,
        *,
        body: SignUpWebauthnVerifyRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Verify Webauthn sign-up\n\nComplete the Webauthn sign-up process by verifying the """
            """response from the user's device. Returns a session if validation is successful.\n"""
            """\nArgs:\n    body (SignUpWebauthnVerifyRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/signup/webauthn/verify"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_up_passwordless_email(
        self,
        *,
        body: SignUpPasswordlessEmailRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Sign up with magic link email\n\nRegister a new user account using passwordless """
            """email authentication. Sends a magic link to the specified email address for """
            """verification.\nUse this endpoint to explicitly register a new account. When """
            """`AUTH_DISABLE_AUTO_SIGNUP` is enabled, this is the only way to register through """
            """this method.\n\n\nArgs:\n    body (SignUpPasswordlessEmailRequest): Request """
            """body.\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/signup/passwordless/email"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_up_otp_email(
        self,
        *,
        body: SignUpOTPEmailRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Sign up with email OTP\n\nRegister a new user account using email OTP """
            """authentication. Sends a one-time password to the specified email address.\nUse """
            """this endpoint to explicitly register a new account. When """
            """`AUTH_DISABLE_AUTO_SIGNUP` is enabled, this is the only way to register through """
            """this method.\n\n\nArgs:\n    body (SignUpOTPEmailRequest): Request body.\n    """
            """headers (Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/signup/otp/email"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_up_passwordless_sms(
        self,
        *,
        body: SignUpPasswordlessSmsRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Sign up with SMS OTP\n\nRegister a new user account using SMS OTP authentication. """
            """Sends a one-time password to the specified phone number.\nUse this endpoint to """
            """explicitly register a new account. When `AUTH_DISABLE_AUTO_SIGNUP` is enabled, """
            """this is the only way to register through this method.\n\n\nArgs:\n    body """
            """(SignUpPasswordlessSmsRequest): Request body.\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    FetchResponse[OKResponse]: """
            """The HTTP response."""
        )
        url = f"{self.base_url}/signup/passwordless/sms"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def sign_up_id_token(
        self,
        *,
        body: SignUpIdTokenRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Sign up with ID token\n\nRegister a new user account using an ID token from Apple """
            """or Google.\nUse this endpoint to explicitly register a new account. When """
            """`AUTH_DISABLE_AUTO_SIGNUP` is enabled, this is the only way to register through """
            """this method.\nIf the user already exists, a `user-already-exists` error is """
            """returned.\n\n\nArgs:\n    body (SignUpIdTokenRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/signup/idtoken"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    def sign_up_provider_url(
        self,
        provider: SignInProvider,
        *,
        params: SignUpProviderParams | None = None,
    ) -> str:
        (
            """Sign up with OAuth provider\n\nInitiate OAuth signup flow with the specified """
            """provider. Redirects to the provider's authorization page.\nUse this endpoint to """
            """explicitly register a new account. When `AUTH_DISABLE_AUTO_SIGNUP` is enabled, """
            """this is the only way to register through this method.\nIf the user already exists """
            """at callback time, they are redirected with `error=user-already-exists`.\n\n\n"""
            """Args:\n    provider (SignInProvider): The name of the social provider\n    params """
            """(SignUpProviderParams): Query and header parameters.\n\nReturns:\n    str: The """
            """redirect URL."""
        )
        url = f"{self.base_url}/signup/provider/{_escape_path(provider)}"

        query: list[tuple[str, str | int | float | bool | None]] = []
        if params is not None and params.allowed_roles is not None:
            query.extend(_query_parameter("allowedRoles", params.allowed_roles, "form", False))
        if params is not None and params.default_role is not None:
            query.extend(_query_parameter("defaultRole", params.default_role, "form", True))
        if params is not None and params.display_name is not None:
            query.extend(_query_parameter("displayName", params.display_name, "form", True))
        if params is not None and params.locale is not None:
            query.extend(_query_parameter("locale", params.locale, "form", True))
        if params is not None and params.metadata is not None:
            query.append(("metadata", to_json(params.metadata)))
        if params is not None and params.redirect_to is not None:
            query.extend(_query_parameter("redirectTo", params.redirect_to, "form", True))
        if params is not None and params.state is not None:
            query.extend(_query_parameter("state", params.state, "form", True))
        if params is not None and params.provider_specific_params is not None:
            query.extend(
                _query_parameter(
                    "providerSpecificParams", params.provider_specific_params, "form", True
                )
            )
        if params is not None and params.upstream_params is not None:
            query.extend(
                _query_parameter("upstreamParams", params.upstream_params, "deepObject", True)
            )
        if params is not None and params.code_challenge is not None:
            query.extend(_query_parameter("codeChallenge", params.code_challenge, "form", True))
        if query:
            return str(self._http.build_request("GET", url, params=query).url)
        return url

    async def refresh_token(
        self,
        *,
        body: RefreshTokenRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[Session]:
        (
            """Refresh access token\n\nGenerate a new JWT access token using a valid refresh """
            """token. The refresh token used will be revoked and a new one will be issued.\n\n"""
            """Args:\n    body (RefreshTokenRequest): Request body.\n    headers (Mapping[str, """
            """str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[Session]: The HTTP response."""
        )
        url = f"{self.base_url}/token"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, Session)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def refresh_provider_token(
        self,
        provider: SignInProvider,
        *,
        body: RefreshProviderTokenRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[ProviderSession]:
        (
            """Refresh OAuth2 provider tokens\n\nRefresh the OAuth2 provider access token using """
            """a valid refresh token. Returns a new provider session with updated access token, """
            """refresh token (if rotated by provider), and expiration information. This endpoint """
            """allows maintaining long-lived access to provider APIs without requiring the user """
            """to re-authenticate.\n\nArgs:\n    provider (SignInProvider): The name of the """
            """social provider\n    body (RefreshProviderTokenRequest): Request body.\n    """
            """headers (Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[ProviderSession]: The HTTP response."""
        )
        url = f"{self.base_url}/token/provider/{_escape_path(provider)}"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, ProviderSession)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_token(
        self,
        *,
        body: VerifyTokenRequest | None = None,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[str]:
        (
            """Verify JWT token\n\nVerify the validity of a JWT access token. If no request body """
            """is provided, the Authorization header will be used for verification.\n\nArgs:\n   """
            """ body (VerifyTokenRequest): Request body.\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    FetchResponse[str]: The HTTP """
            """response."""
        )
        url = f"{self.base_url}/token/verify"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, str)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def get_user(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[User]:
        (
            """Get user information\n\nRetrieve the authenticated user's profile information """
            """including roles, metadata, and account status.\n\nArgs:\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[User]: The HTTP response."""
        )
        url = f"{self.base_url}/user"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, User)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def deanonymize_user(
        self,
        *,
        body: UserDeanonymizeRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Deanonymize an anonymous user\n\nConvert an anonymous user to a regular user by """
            """adding email and optionally password credentials. A confirmation email will be """
            """sent if the server is configured to do so.\n\nArgs:\n    body """
            """(UserDeanonymizeRequest): Request body.\n    headers (Mapping[str, str] | None): """
            """Additional request headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP """
            """response."""
        )
        url = f"{self.base_url}/user/deanonymize"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def deanonymize_user_sms(
        self,
        *,
        body: UserDeanonymizeSmsRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Deanonymize an anonymous user with SMS OTP\n\nConvert an anonymous user to a """
            """regular user by adding a phone number. A one-time password is sent to the\nphone """
            """number; the user completes verification by calling `/signin/passwordless/sms/otp` """
            """with the OTP, which\nmarks the phone number as verified and returns a session.\n\n"""
            """\nArgs:\n    body (UserDeanonymizeSmsRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/deanonymize/sms"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def change_user_email(
        self,
        *,
        body: UserEmailChangeRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Change user email\n\nRequest to change the authenticated user's email address. A """
            """verification email will be sent to the new address to confirm the change. """
            """Requires elevated permissions.\n\nArgs:\n    body (UserEmailChangeRequest): """
            """Request body.\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/email/change"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def change_user_phone_number(
        self,
        *,
        body: UserPhoneNumberChangeRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Change user phone number\n\nRequest to change the authenticated user's phone """
            """number. A one-time password is sent\nvia SMS to the new phone number; complete """
            """the change by calling\n`/user/phone-number/change/verify` with the OTP. The """
            """current `phone_number` is left\nunchanged until verification succeeds. Requires """
            """elevated permissions.\n\n\nArgs:\n    body (UserPhoneNumberChangeRequest): """
            """Request body.\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/phone-number/change"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_change_user_phone_number(
        self,
        *,
        body: UserPhoneNumberChangeVerifyRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Verify phone number change\n\nComplete a previously-requested phone number change """
            """by submitting the OTP that was\nsent via SMS. On success the staged phone number """
            """becomes the user's verified phone\nnumber. Requires elevated permissions.\n\n\n"""
            """Args:\n    body (UserPhoneNumberChangeVerifyRequest): Request body.\n    headers """
            """(Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/phone-number/change/verify"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def send_verification_email(
        self,
        *,
        body: UserEmailSendVerificationEmailRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Send verification email\n\nSend an email verification link to the specified email """
            """address. Used to verify email addresses for new accounts or email changes.\n\n"""
            """Args:\n    body (UserEmailSendVerificationEmailRequest): Request body.\n    """
            """headers (Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/email/send-verification-email"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_change_user_mfa(
        self,
        *,
        body: UserMfaRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Manage multi-factor authentication\n\nActivate or deactivate multi-factor """
            """authentication for the authenticated user\n\nArgs:\n    body (UserMfaRequest): """
            """Request body.\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/mfa"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def change_user_password(
        self,
        *,
        body: UserPasswordRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Change user password\n\nChange the user's password. The user must be """
            """authenticated with elevated permissions or provide a valid password reset """
            """ticket.\n\nAll of the user's existing sessions are revoked atomically as part of """
            """this operation, including the session used to make the request. Clients must """
            """treat the user as signed out after a successful response and obtain a new session """
            """via sign-in.\n\n\nArgs:\n    body (UserPasswordRequest): Request body.\n    """
            """headers (Mapping[str, str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/password"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def send_password_reset_email(
        self,
        *,
        body: UserPasswordResetRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OKResponse]:
        (
            """Request password reset\n\nRequest a password reset for a user account. An email """
            """with a verification link will be sent to the user's email address to complete the """
            """password reset process.\n\nArgs:\n    body (UserPasswordResetRequest): Request """
            """body.\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[OKResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/password/reset"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OKResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def add_security_key(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[PublicKeyCredentialCreationOptions]:
        (
            """Initialize adding of a new webauthn security key\n\nStart the process of adding a """
            """new WebAuthn security key to the user's account. Returns a challenge that must be """
            """completed by the user's authenticator device. Requires elevated permissions.\n\n"""
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[PublicKeyCredentialCreationOptions]: The HTTP """
            """response."""
        )
        url = f"{self.base_url}/user/webauthn/add"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, PublicKeyCredentialCreationOptions)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def verify_add_security_key(
        self,
        *,
        body: VerifyAddSecurityKeyRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[VerifyAddSecurityKeyResponse]:
        (
            """Verify adding of a new webauthn security key\n\nComplete the process of adding a """
            """new WebAuthn security key by verifying the authenticator response. Requires """
            """elevated permissions.\n\nArgs:\n    body (VerifyAddSecurityKeyRequest): Request """
            """body.\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[VerifyAddSecurityKeyResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/user/webauthn/verify"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, VerifyAddSecurityKeyResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def token_exchange(
        self,
        *,
        body: TokenExchangeRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[SessionPayload]:
        (
            """Exchange authorization code for session\n\nExchange an authorization code """
            """(obtained via PKCE flow) together with the original code_verifier for a session """
            """containing access and refresh tokens.\n\nArgs:\n    body (TokenExchangeRequest): """
            """Request body.\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[SessionPayload]: The HTTP response."""
        )
        url = f"{self.base_url}/token/exchange"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, SessionPayload)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    def verify_ticket_url(
        self,
        *,
        params: VerifyTicketParams,
    ) -> str:
        (
            """Verify email and authentication tickets\n\nVerify tickets created by email """
            """verification, magic link authentication, or password reset processes. Redirects """
            """the user to the appropriate destination upon successful verification.\n\nArgs:\n  """
            """  params (VerifyTicketParams): Query and header parameters.\n\nReturns:\n    str: """
            """The redirect URL."""
        )
        url = f"{self.base_url}/verify"

        query: list[tuple[str, str | int | float | bool | None]] = []
        query.extend(_query_parameter("ticket", params.ticket, "form", True))
        if params.type is not None:
            query.extend(_query_parameter("type", params.type, "form", True))
        query.extend(_query_parameter("redirectTo", params.redirect_to, "form", True))
        if params.code_challenge is not None:
            query.extend(_query_parameter("codeChallenge", params.code_challenge, "form", True))
        if query:
            return str(self._http.build_request("GET", url, params=query).url)
        return url

    async def get_version(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[GetVersionResponse200]:
        (
            """Get service version\n\nRetrieve version information about the authentication """
            """service\n\nArgs:\n    headers (Mapping[str, str] | None): Additional request """
            """headers.\n\nReturns:\n    FetchResponse[GetVersionResponse200]: The HTTP response."""
        )
        url = f"{self.base_url}/version"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, GetVersionResponse200)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def get_open_id_configuration(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2DiscoveryResponse]:
        (
            """OpenID Connect Discovery\n\nReturns the OpenID Provider Metadata (RFC 8414)\n\n"""
            """Args:\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[OAuth2DiscoveryResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/.well-known/openid-configuration"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2DiscoveryResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def get_o_auth_authorization_server(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2DiscoveryResponse]:
        (
            """OAuth2 Authorization Server Metadata\n\nReturns the Authorization Server Metadata """
            """(RFC 8414). Same content as OpenID Discovery.\n\nArgs:\n    headers (Mapping[str, """
            """str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OAuth2DiscoveryResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/.well-known/oauth-authorization-server"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2DiscoveryResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    def oauth2_authorize_url(
        self,
        *,
        params: Oauth2AuthorizeParams,
    ) -> str:
        (
            """OAuth2 Authorization Endpoint\n\nInitiates an OAuth2 authorization code flow. """
            """Validates the request and redirects to the login UI for user authentication and """
            """consent.\n\nArgs:\n    params (Oauth2AuthorizeParams): Query and header """
            """parameters.\n\nReturns:\n    str: The redirect URL."""
        )
        url = f"{self.base_url}/oauth2/authorize"

        query: list[tuple[str, str | int | float | bool | None]] = []
        query.extend(_query_parameter("client_id", params.client_id, "form", True))
        query.extend(_query_parameter("redirect_uri", params.redirect_uri, "form", True))
        query.extend(_query_parameter("response_type", params.response_type, "form", True))
        if params.scope is not None:
            query.extend(_query_parameter("scope", params.scope, "form", True))
        if params.state is not None:
            query.extend(_query_parameter("state", params.state, "form", True))
        if params.nonce is not None:
            query.extend(_query_parameter("nonce", params.nonce, "form", True))
        if params.code_challenge is not None:
            query.extend(_query_parameter("code_challenge", params.code_challenge, "form", True))
        if params.code_challenge_method is not None:
            query.extend(
                _query_parameter(
                    "code_challenge_method", params.code_challenge_method, "form", True
                )
            )
        if params.resource is not None:
            query.extend(_query_parameter("resource", params.resource, "form", True))
        if params.prompt is not None:
            query.extend(_query_parameter("prompt", params.prompt, "form", True))
        if query:
            return str(self._http.build_request("GET", url, params=query).url)
        return url

    def oauth2_authorize_post_url(
        self,
    ) -> str:
        (
            """OAuth2 Authorization Endpoint (POST)\n\nInitiates an OAuth2 authorization code """
            """flow via POST. Validates the request and redirects to the login UI for user """
            """authentication and consent.\n\nReturns:\n    str: The redirect URL."""
        )
        url = f"{self.base_url}/oauth2/authorize"
        return url

    async def oauth2_token(
        self,
        *,
        body: OAuth2TokenRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2TokenResponse]:
        (
            """OAuth2 Token Endpoint\n\nExchange an authorization code for tokens, or refresh an """
            """existing token. Supports grant_type authorization_code and refresh_token.\n\n"""
            """Args:\n    body (OAuth2TokenRequest): Request body.\n    headers (Mapping[str, """
            """str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OAuth2TokenResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/token"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            data=to_jsonable(body),
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2TokenResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def oauth2_userinfo_get(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2UserinfoResponse]:
        (
            """OpenID Connect UserInfo Endpoint (GET)\n\nReturns claims about the authenticated """
            """user based on the access token scopes.\n\nArgs:\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OAuth2UserinfoResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/userinfo"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2UserinfoResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def oauth2_userinfo_post(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2UserinfoResponse]:
        (
            """OpenID Connect UserInfo Endpoint (POST)\n\nReturns claims about the authenticated """
            """user based on the access token scopes.\n\nArgs:\n    headers (Mapping[str, str] | """
            """None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OAuth2UserinfoResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/userinfo"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2UserinfoResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def oauth2_jwks(
        self,
        *,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2JWKSResponse]:
        (
            """OAuth2 Provider JWKS Endpoint\n\nReturns the JSON Web Key Set containing public """
            """keys used for OAuth2/OIDC token signing.\n\nArgs:\n    headers (Mapping[str, str] """
            """| None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OAuth2JWKSResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/jwks"
        query = None
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2JWKSResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def oauth2_revoke(
        self,
        *,
        body: OAuth2RevokeRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[None]:
        (
            """OAuth2 Token Revocation (RFC 7009)\n\nRevoke an access token or refresh token.\n\n"""
            """Args:\n    body (OAuth2RevokeRequest): Request body.\n    headers (Mapping[str, """
            """str] | None): Additional request headers.\n\nReturns:\n    FetchResponse[None]: """
            """The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/revoke"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            data=to_jsonable(body),
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = None
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def oauth2_introspect(
        self,
        *,
        body: OAuth2IntrospectRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2IntrospectResponse]:
        (
            """OAuth2 Token Introspection (RFC 7662)\n\nIntrospect a token to determine its """
            """current state and metadata.\n\nArgs:\n    body (OAuth2IntrospectRequest): Request """
            """body.\n    headers (Mapping[str, str] | None): Additional request headers.\n\n"""
            """Returns:\n    FetchResponse[OAuth2IntrospectResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/introspect"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            data=to_jsonable(body),
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2IntrospectResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def oauth2_login_get(
        self,
        *,
        params: Oauth2LoginGetParams,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2LoginResponse]:
        (
            """Get authorization request details for consent screen\n\nCalled by the consent UI """
            """to get details about the pending authorization request.\n\nArgs:\n    params """
            """(Oauth2LoginGetParams): Query and header parameters.\n    headers (Mapping[str, """
            """str] | None): Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OAuth2LoginResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/login"

        query: list[tuple[str, str | int | float | bool | None]] = []
        query.extend(_query_parameter("request_id", params.request_id, "form", True))
        request = self._http.build_request(
            "GET",
            url,
            params=query,
            headers=headers,
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2LoginResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)

    async def oauth2_login_post(
        self,
        *,
        body: OAuth2LoginRequest,
        headers: Mapping[str, str] | None = None,
    ) -> FetchResponse[OAuth2LoginCompleteResponse]:
        (
            """Complete login/consent for an authorization request\n\nCalled by the consent UI """
            """after user authenticates and consents. Sets the user on the auth request and """
            """redirects back to the client with an authorization code.\n\nArgs:\n    body """
            """(OAuth2LoginRequest): Request body.\n    headers (Mapping[str, str] | None): """
            """Additional request headers.\n\nReturns:\n    """
            """FetchResponse[OAuth2LoginCompleteResponse]: The HTTP response."""
        )
        url = f"{self.base_url}/oauth2/login"
        query = None
        request = self._http.build_request(
            "POST",
            url,
            params=query,
            json=to_jsonable(body),
            headers={"Content-Type": "application/json", **(headers or {})},
        )
        response = await self._fetch(request)
        if response.status_code >= _MIN_ERROR_STATUS:
            raise HTTPError.from_response(response)
        payload = decode_json(response, OAuth2LoginCompleteResponse)
        return FetchResponse(body=payload, status=response.status_code, headers=response.headers)


__all__ = [
    "AttestationFormat",
    "AuthenticationExtensionsClientOutputs",
    "AuthenticatorAssertionResponse",
    "AuthenticatorAttachment",
    "AuthenticatorAttestationResponse",
    "AuthenticatorSelection",
    "AuthenticatorTransport",
    "ConveyancePreference",
    "CreatePATRequest",
    "CreatePATResponse",
    "CredentialAssertionResponse",
    "CredentialCreationResponse",
    "CredentialParameter",
    "CredentialPropertiesOutput",
    "CredentialType",
    "ErrorResponseError",
    "ErrorResponse",
    "IdTokenProvider",
    "JWK",
    "JWKSet",
    "LinkIdTokenRequest",
    "MFAChallengePayload",
    "OKResponse",
    "OptionsRedirectTo",
    "PublicKeyCredentialCreationOptions",
    "PublicKeyCredentialDescriptor",
    "PublicKeyCredentialHints",
    "PublicKeyCredentialRequestOptions",
    "ProviderSession",
    "ProviderSpecificParams",
    "RefreshProviderTokenRequest",
    "RefreshTokenRequest",
    "RelyingPartyEntity",
    "ResidentKeyRequirement",
    "Session",
    "SessionPayload",
    "SignInAnonymousRequest",
    "SignInEmailPasswordRequest",
    "SignInEmailPasswordResponse",
    "SignInIdTokenRequest",
    "SignInMfaTotpRequest",
    "SignInOTPEmailRequest",
    "SignInOTPEmailVerifyRequest",
    "SignInOTPEmailVerifyResponse",
    "SignInPATRequest",
    "SignInPasswordlessEmailRequest",
    "SignInPasswordlessSmsOtpRequest",
    "SignInPasswordlessSmsOtpResponse",
    "SignInPasswordlessSmsRequest",
    "SignUpPasswordlessEmailRequest",
    "SignUpOTPEmailRequest",
    "SignUpPasswordlessSmsRequest",
    "SignUpIdTokenRequest",
    "SignInWebauthnRequest",
    "SignInWebauthnVerifyRequest",
    "SignOutRequest",
    "SignUpEmailPasswordRequest",
    "SignUpOptions",
    "SignUpWebauthnRequest",
    "SignUpWebauthnVerifyRequest",
    "TokenExchangeRequest",
    "TotpGenerateResponse",
    "URLEncodedBase64",
    "User",
    "UserDeanonymizeRequestSignInMethod",
    "UserDeanonymizeRequest",
    "UserDeanonymizeSmsRequest",
    "UserEmailChangeRequest",
    "UserPhoneNumberChangeRequest",
    "UserPhoneNumberChangeVerifyRequest",
    "UserEmailSendVerificationEmailRequest",
    "UserEntity",
    "UserMfaRequestActiveMfaType",
    "UserMfaRequest",
    "UserPasswordRequest",
    "UserPasswordResetRequest",
    "UserVerificationRequirement",
    "VerifyAddSecurityKeyRequest",
    "VerifyAddSecurityKeyResponse",
    "VerifyTokenRequest",
    "OAuth2ErrorResponse",
    "OAuth2DiscoveryResponse",
    "OAuth2TokenRequestGrantType",
    "OAuth2TokenRequest",
    "OAuth2TokenResponse",
    "OAuth2UserinfoResponse",
    "OAuth2JWKSResponse",
    "OAuth2RevokeRequestTokenTypeHint",
    "OAuth2RevokeRequest",
    "OAuth2IntrospectRequestTokenTypeHint",
    "OAuth2IntrospectRequest",
    "OAuth2IntrospectResponse",
    "OAuth2LoginResponse",
    "OAuth2LoginRequest",
    "OAuth2LoginCompleteResponse",
    "RedirectToQuery",
    "SignInProvider",
    "TicketQuery",
    "TicketTypeQuery",
    "GetVersionResponse200",
    "GetCodeChallengeMethod",
    "Oauth2AuthorizePostBody",
    "SignInProviderParams",
    "SignUpProviderParams",
    "VerifyTicketParams",
    "Oauth2AuthorizeParams",
    "Oauth2LoginGetParams",
    "Client",
]
