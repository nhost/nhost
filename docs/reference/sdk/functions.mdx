---
title: 'Functions'
---

## `nhost.functions.call()`

Send a request to a cloud function:

```js
const { res, error } = await nhost.functions.call('/test', { name: 'Johan' })
```

If the user is signed in, the the user's access token will automaticaly be sent in the `Authorization` header.
