---
title: createPAT()
sidebarTitle: createPAT()
---

Use `nhost.auth.createPAT` to create a personal access token for the user.

## Parameters

---

**<span className="parameter-name">expiresAt</span>** <span className="optional-status">required</span> <code>Date</code>

Expiration date for the token

---

**<span className="parameter-name">metadata</span>** <span className="optional-status">optional</span> <code>Record&lt;string, string &#124; number&gt;</code>

Optional metadata to store with the token

---
