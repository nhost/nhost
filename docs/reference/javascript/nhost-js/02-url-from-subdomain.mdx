---
title: urlFromSubdomain()
sidebarTitle: urlFromSubdomain()
---

\`backendUrl\` should now be used only when self-hosting
\`subdomain\` and `region` should be used instead when using the Nhost platform

**`@returns`**

## Parameters

---

**<span className="parameter-name">backendOrSubdomain</span>** <span className="optional-status">required</span> <code>Pick&lt;NhostClientConstructorParams, "backendUrl" &#124; "subdomain" &#124; "region"&gt;</code>

---

**<span className="parameter-name">service</span>** <span className="optional-status">required</span> <code>string</code>

---
