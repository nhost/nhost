---
title: useChangeEmail()
sidebarTitle: useChangeEmail()
---

Use the composable `useChangeEmail` to change email for the user.

```tsx
const { changeEmail, isLoading, needsEmailVerification, isError, error } =
  useChangeEmail()

watchEffect(() => {
  console.log(
    isLoading.value,
    needsEmailVerification.value,
    isError.value,
    error.value
  )
})

const handleFormSubmit = async (e) => {
  e.preventDefault()

  await changeEmail('new@example.com')
}
```

## Parameters

---

**<span className="parameter-name">options</span>** <span className="optional-status">optional</span> <code>NestedRefOfValue&lt;undefined &#124; ChangeEmailOptions&gt;</code>

---
