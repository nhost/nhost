---
title: Upgrade Major Version
description: Upgrade your Nhost PostgreSQL major version to 18 from the dashboard, with notes on the process and required downtime.
keywords: ["PostgreSQL", "postgres upgrade", "major version", "database migration", "pg_upgrade", "downtime"]
head:
  - tag: title
    content: "Upgrading Your PostgreSQL Major Version | Nhost Docs"
---

# Upgrade process

:::note
This document only applies when changing the Postgres major version (for example, from 17 to 18). It doesn't apply when upgrading minor versions (for example, from 18.5 to 18.6).
:::

Nhost supports Postgres 18. To upgrade to Postgres 18, go to [Database → Settings → Postgres version](https://app.nhost.io/orgs/_/projects/_/database/settings?tab=version), select the new major version, and start the process:

![dashboard settings](/images/database/upgrade_01.png)

:::caution
Keep in mind that the upgrade process requires downtime. Pay attention to all the information provided to you in the settings page.
:::

After starting the process you can follow it on the same page:

![logs](/images/database/upgrade_02.png)

Finally, you can confirm the upgrade by executing the SQL query `SELECT version();`

![select version()](/images/database/upgrade_03.png)

## Projects with connected repos

This process can only be triggered from the dashboard. If you have a project with a connected repository and want to upgrade postgres to a new major version you will have to follow the steps below:

1. Upgrade the major version using the dashboard
2. Run `nhost config pull` or edit the `nhost.toml` by hand.
3. Push to git (this step should be a NOOP and can be skipped)

If you attempt to change major versions via a deployment the deployment will fail. This is done on purpose to avoid unintended upgrades which can lead to downtime.
