---
title: Migrate to Nhost Config
description: Migrate legacy Nhost projects to the new nhost.toml config file by linking your project and pulling its current configuration.
keywords: ["migration", "config pull", "nhost.toml", "project linking", "configuration migration"]
head:
  - tag: title
    content: "Upgrade Legacy Projects to the New Config File | Nhost Docs"
---

import { AsciinemaPlayer } from '@components';

If you want to migrate an existing project you need to:

1. Go into your project's folder:

```bash frame="none"
$ cd /path/to/existingproject
```

2. Login:

```bash frame="none"
$ nhost login
- email: david@nhost.io
- password:
Authenticating
Successfully logged in, creating PAT
Successfully created PAT
Storing PAT for future user
```

3. Link your project:

```bash frame="none"
$ nhost link
```

<AsciinemaPlayer src="/casts/cli/nhost-link.cast" poster="npt:0:07" idleTimeLimit={2} />

4. Pull the configuration from the cloud:

```bash frame="none"
$ nhost config pull
```

<AsciinemaPlayer src="/casts/cli/nhost-config-pull.cast" poster="npt:0:08" idleTimeLimit={2} />

5. Follow the instructions on screen to ensure your configuration contains no secrets and you don't commit sensitive information to git.

6. Profit:

```bash frame="none"
$ nhost up
```

<AsciinemaPlayer src="/casts/cli/nhost-up.cast" poster="npt:0:14" idleTimeLimit={2} />

