---
title: useAuthenticationStatus()
sidebar:
  label: useAuthenticationStatus()
---

Use `useAuthenticationStatus` to get the authentication status for the user.

```tsx
const { isAuthenticated, isLoading } = useAuthenticationStatus()
```
