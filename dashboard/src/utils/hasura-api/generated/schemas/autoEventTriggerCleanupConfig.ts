/**
 * Generated by orval v7.11.2 🍺
 * Do not edit manually.
 * Hasura API
 * API for managing remote schemas and events in Hasura
 * OpenAPI spec version: 1.0.0
 */

/**
 * Auto-cleanup configuration for event logs
 */
export interface AutoEventTriggerCleanupConfig {
  /** Cron expression at which the cleanup should be invoked. */
  schedule: string;
  /** Event logs retention period (in hours). 168 hours of retention period means that the logs older than 7 days will be removed. */
  clear_older_than: number;
  /** Maximum number of logs to delete in a single statement during the cleanup action. If there are more events to be cleaned than the batch_size then the cleanup action will execute multiple statements sequentially until all old event logs are cleared. */
  batch_size?: number;
  /** Maximum time (in seconds) that a batch can take during the cleanup process. If a batch times out, the cleanup process is halted. */
  timeout?: number;
  /** Should corresponding invocation logs be cleaned. */
  clean_invocation_logs?: boolean;
  /** Is the auto-cleanup process paused. */
  paused?: boolean;
}
