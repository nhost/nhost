import { TabsContent } from '@/components/ui/v3/tabs';

function ImportBackupContent() {
  return (
    <TabsContent value="importBackup">
      <h1>Import backup</h1>
      TODO: Fetch projects useGetProjectsQuery
    </TabsContent>
  );
}

export default ImportBackupContent;
