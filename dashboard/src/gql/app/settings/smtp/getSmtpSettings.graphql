query GetSmtpSettings($appId: uuid!) {
  config(appID: $appId, resolve: false) {
    id: __typename
    provider {
      smtp {
        host
        method
        port
        secure
        sender
        user
      }
    }
  }
}
