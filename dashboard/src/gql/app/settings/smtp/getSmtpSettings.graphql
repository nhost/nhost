query GetSmtpSettings($appId: uuid!) {
  config(appID: $appId, resolve: true) {
    id: __typename
    provider {
      smtp {
        host
        method
        port
        secure
        sender
        user
      }
    }
  }
}
