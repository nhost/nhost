query getConfiguredVersions{
  config(appID:"$appid", resolve: true) {
    auth {
      version
    }
    postgres {
      version
    }
    hasura {
      version
    }
    ai {
      version
    }
    storage {
      version
    }
  }
}
