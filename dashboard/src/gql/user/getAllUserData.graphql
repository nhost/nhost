query getAllUserData {
  workspaceMembers {
    id
    workspace {
      id
      name
      creatorUserId
      apps {
        id
        name
        hasuraGraphqlAdminSecret
        subdomain
      }
    }
  }
}
