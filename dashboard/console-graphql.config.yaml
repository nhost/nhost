schema:
  - https://local.graphql.nhost.run/v1:
      headers:
        x-hasura-admin-secret: nhost-admin-secret
        x-hasura-role: user
generates:
  src/utils/__generated__/console-graphql.ts:
    documents:
      - 'src/gql/console/**/*.graphql'
      - 'src/gql/console/**/*.gql'
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-react-apollo'
    config:
      withRefetchFn: true
